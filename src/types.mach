# vk.types: base handle and function-pointer type aliases for core Vulkan (generated).
#
# opaque handles (dispatchable and non-dispatchable alike) are pointer-sized and
# declared as ptr aliases so a consumer names them by their Vulkan identity; the
# PFN_vk* callback signatures become fun-type aliases. scalar base types
# (VkBool32, VkDeviceSize, VkFlags, ...) and enum/bitmask types resolve straight
# to mach scalars at their use sites and carry no alias here.
#
# GENERATED by tools/gen.py from the pinned tools/vk.xml — do not edit by hand.

# Instance: the VkInstance handle.
pub def Instance: ptr;
# PhysicalDevice: the VkPhysicalDevice handle.
pub def PhysicalDevice: ptr;
# Device: the VkDevice handle.
pub def Device: ptr;
# Queue: the VkQueue handle.
pub def Queue: ptr;
# CommandBuffer: the VkCommandBuffer handle.
pub def CommandBuffer: ptr;
# DeviceMemory: the VkDeviceMemory handle.
pub def DeviceMemory: ptr;
# CommandPool: the VkCommandPool handle.
pub def CommandPool: ptr;
# Buffer: the VkBuffer handle.
pub def Buffer: ptr;
# BufferView: the VkBufferView handle.
pub def BufferView: ptr;
# Image: the VkImage handle.
pub def Image: ptr;
# ImageView: the VkImageView handle.
pub def ImageView: ptr;
# ShaderModule: the VkShaderModule handle.
pub def ShaderModule: ptr;
# Pipeline: the VkPipeline handle.
pub def Pipeline: ptr;
# PipelineLayout: the VkPipelineLayout handle.
pub def PipelineLayout: ptr;
# Sampler: the VkSampler handle.
pub def Sampler: ptr;
# DescriptorSet: the VkDescriptorSet handle.
pub def DescriptorSet: ptr;
# DescriptorSetLayout: the VkDescriptorSetLayout handle.
pub def DescriptorSetLayout: ptr;
# DescriptorPool: the VkDescriptorPool handle.
pub def DescriptorPool: ptr;
# Fence: the VkFence handle.
pub def Fence: ptr;
# Semaphore: the VkSemaphore handle.
pub def Semaphore: ptr;
# Event: the VkEvent handle.
pub def Event: ptr;
# QueryPool: the VkQueryPool handle.
pub def QueryPool: ptr;
# Framebuffer: the VkFramebuffer handle.
pub def Framebuffer: ptr;
# RenderPass: the VkRenderPass handle.
pub def RenderPass: ptr;
# PipelineCache: the VkPipelineCache handle.
pub def PipelineCache: ptr;
# DescriptorUpdateTemplate: the VkDescriptorUpdateTemplate handle.
pub def DescriptorUpdateTemplate: ptr;
# SamplerYcbcrConversion: the VkSamplerYcbcrConversion handle.
pub def SamplerYcbcrConversion: ptr;
# PrivateDataSlot: the VkPrivateDataSlot handle.
pub def PrivateDataSlot: ptr;

# InternalAllocationNotification: the PFN_vkInternalAllocationNotification callback signature.
pub def InternalAllocationNotification: fun(ptr, u64, i32, i32);
# InternalFreeNotification: the PFN_vkInternalFreeNotification callback signature.
pub def InternalFreeNotification: fun(ptr, u64, i32, i32);
# ReallocationFunction: the PFN_vkReallocationFunction callback signature.
pub def ReallocationFunction: fun(ptr, ptr, u64, u64, i32) ptr;
# AllocationFunction: the PFN_vkAllocationFunction callback signature.
pub def AllocationFunction: fun(ptr, u64, u64, i32) ptr;
# FreeFunction: the PFN_vkFreeFunction callback signature.
pub def FreeFunction: fun(ptr, ptr);
# VoidFunction: the PFN_vkVoidFunction callback signature.
pub def VoidFunction: fun();

test "types: a handle is pointer-sized and opaque" {
    if ($size_of(Instance) != 8) { ret 1; }
    if ($size_of(Device) != 8) { ret 1; }
    ret 0;
}
