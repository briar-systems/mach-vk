# raw loaded-pointer layer for core Vulkan (generated)
#
# one pub var function pointer per core command, nil until a resolver fills it,
# carrying the C name and a C-faithful signature. a command the running
# implementation does not export stays nil; the same contract as in C.
#
# resolution walks the standard chain: load_global resolves the instanceless
# global commands through the caller's bootstrap vkGetInstanceProcAddr,
# load_instance resolves instance-level commands once an instance exists, and
# load_device narrows device-level commands through vkGetDeviceProcAddr for
# dispatch without the instance trampoline.
#
# GENERATED by tools/gen.py from the pinned tools/vk.xml; do not edit by hand.

use vk.types;
use vk.structs;

# resolves a command name against an instance (nil for the global commands),
# returning the entry point or nil. mirrors PFN_vkGetInstanceProcAddr; the
# bootstrap the caller provides
pub def GetInstanceProcAddr: fun(ptr, *u8) ptr;

# resolves a command name against a device, returning the entry point or nil.
# mirrors PFN_vkGetDeviceProcAddr; the narrowed dispatch path for device-level
# commands
pub def GetDeviceProcAddr: fun(ptr, *u8) ptr;

# loaded pointer for the global-level command vkCreateInstance
pub var vkCreateInstance: fun(*structs.InstanceCreateInfo, *structs.AllocationCallbacks, *types.Instance) i32 = nil;
# loaded pointer for the instance-level command vkDestroyInstance
pub var vkDestroyInstance: fun(types.Instance, *structs.AllocationCallbacks) = nil;
# loaded pointer for the instance-level command vkEnumeratePhysicalDevices
pub var vkEnumeratePhysicalDevices: fun(types.Instance, *u32, *types.PhysicalDevice) i32 = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceFeatures
pub var vkGetPhysicalDeviceFeatures: fun(types.PhysicalDevice, *structs.PhysicalDeviceFeatures) = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceFormatProperties
pub var vkGetPhysicalDeviceFormatProperties: fun(types.PhysicalDevice, i32, *structs.FormatProperties) = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceImageFormatProperties
pub var vkGetPhysicalDeviceImageFormatProperties: fun(types.PhysicalDevice, i32, i32, i32, u32, u32, *structs.ImageFormatProperties) i32 = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceProperties
pub var vkGetPhysicalDeviceProperties: fun(types.PhysicalDevice, *structs.PhysicalDeviceProperties) = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceQueueFamilyProperties
pub var vkGetPhysicalDeviceQueueFamilyProperties: fun(types.PhysicalDevice, *u32, *structs.QueueFamilyProperties) = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceMemoryProperties
pub var vkGetPhysicalDeviceMemoryProperties: fun(types.PhysicalDevice, *structs.PhysicalDeviceMemoryProperties) = nil;
# loaded pointer for the instance-level command vkGetInstanceProcAddr
pub var vkGetInstanceProcAddr: fun(types.Instance, *u8) types.VoidFunction = nil;
# loaded pointer for the device-level command vkGetDeviceProcAddr
pub var vkGetDeviceProcAddr: fun(types.Device, *u8) types.VoidFunction = nil;
# loaded pointer for the instance-level command vkCreateDevice
pub var vkCreateDevice: fun(types.PhysicalDevice, *structs.DeviceCreateInfo, *structs.AllocationCallbacks, *types.Device) i32 = nil;
# loaded pointer for the device-level command vkDestroyDevice
pub var vkDestroyDevice: fun(types.Device, *structs.AllocationCallbacks) = nil;
# loaded pointer for the global-level command vkEnumerateInstanceExtensionProperties
pub var vkEnumerateInstanceExtensionProperties: fun(*u8, *u32, *structs.ExtensionProperties) i32 = nil;
# loaded pointer for the instance-level command vkEnumerateDeviceExtensionProperties
pub var vkEnumerateDeviceExtensionProperties: fun(types.PhysicalDevice, *u8, *u32, *structs.ExtensionProperties) i32 = nil;
# loaded pointer for the global-level command vkEnumerateInstanceLayerProperties
pub var vkEnumerateInstanceLayerProperties: fun(*u32, *structs.LayerProperties) i32 = nil;
# loaded pointer for the instance-level command vkEnumerateDeviceLayerProperties
pub var vkEnumerateDeviceLayerProperties: fun(types.PhysicalDevice, *u32, *structs.LayerProperties) i32 = nil;
# loaded pointer for the device-level command vkGetDeviceQueue
pub var vkGetDeviceQueue: fun(types.Device, u32, u32, *types.Queue) = nil;
# loaded pointer for the device-level command vkQueueSubmit
pub var vkQueueSubmit: fun(types.Queue, u32, *structs.SubmitInfo, types.Fence) i32 = nil;
# loaded pointer for the device-level command vkQueueWaitIdle
pub var vkQueueWaitIdle: fun(types.Queue) i32 = nil;
# loaded pointer for the device-level command vkDeviceWaitIdle
pub var vkDeviceWaitIdle: fun(types.Device) i32 = nil;
# loaded pointer for the device-level command vkAllocateMemory
pub var vkAllocateMemory: fun(types.Device, *structs.MemoryAllocateInfo, *structs.AllocationCallbacks, *types.DeviceMemory) i32 = nil;
# loaded pointer for the device-level command vkFreeMemory
pub var vkFreeMemory: fun(types.Device, types.DeviceMemory, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkMapMemory
pub var vkMapMemory: fun(types.Device, types.DeviceMemory, u64, u64, u32, *ptr) i32 = nil;
# loaded pointer for the device-level command vkUnmapMemory
pub var vkUnmapMemory: fun(types.Device, types.DeviceMemory) = nil;
# loaded pointer for the device-level command vkFlushMappedMemoryRanges
pub var vkFlushMappedMemoryRanges: fun(types.Device, u32, *structs.MappedMemoryRange) i32 = nil;
# loaded pointer for the device-level command vkInvalidateMappedMemoryRanges
pub var vkInvalidateMappedMemoryRanges: fun(types.Device, u32, *structs.MappedMemoryRange) i32 = nil;
# loaded pointer for the device-level command vkGetDeviceMemoryCommitment
pub var vkGetDeviceMemoryCommitment: fun(types.Device, types.DeviceMemory, *u64) = nil;
# loaded pointer for the device-level command vkBindBufferMemory
pub var vkBindBufferMemory: fun(types.Device, types.Buffer, types.DeviceMemory, u64) i32 = nil;
# loaded pointer for the device-level command vkBindImageMemory
pub var vkBindImageMemory: fun(types.Device, types.Image, types.DeviceMemory, u64) i32 = nil;
# loaded pointer for the device-level command vkGetBufferMemoryRequirements
pub var vkGetBufferMemoryRequirements: fun(types.Device, types.Buffer, *structs.MemoryRequirements) = nil;
# loaded pointer for the device-level command vkGetImageMemoryRequirements
pub var vkGetImageMemoryRequirements: fun(types.Device, types.Image, *structs.MemoryRequirements) = nil;
# loaded pointer for the device-level command vkGetImageSparseMemoryRequirements
pub var vkGetImageSparseMemoryRequirements: fun(types.Device, types.Image, *u32, *structs.SparseImageMemoryRequirements) = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceSparseImageFormatProperties
pub var vkGetPhysicalDeviceSparseImageFormatProperties: fun(types.PhysicalDevice, i32, i32, i32, u32, i32, *u32, *structs.SparseImageFormatProperties) = nil;
# loaded pointer for the device-level command vkQueueBindSparse
pub var vkQueueBindSparse: fun(types.Queue, u32, *structs.BindSparseInfo, types.Fence) i32 = nil;
# loaded pointer for the device-level command vkCreateFence
pub var vkCreateFence: fun(types.Device, *structs.FenceCreateInfo, *structs.AllocationCallbacks, *types.Fence) i32 = nil;
# loaded pointer for the device-level command vkDestroyFence
pub var vkDestroyFence: fun(types.Device, types.Fence, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkResetFences
pub var vkResetFences: fun(types.Device, u32, *types.Fence) i32 = nil;
# loaded pointer for the device-level command vkGetFenceStatus
pub var vkGetFenceStatus: fun(types.Device, types.Fence) i32 = nil;
# loaded pointer for the device-level command vkWaitForFences
pub var vkWaitForFences: fun(types.Device, u32, *types.Fence, u32, u64) i32 = nil;
# loaded pointer for the device-level command vkCreateSemaphore
pub var vkCreateSemaphore: fun(types.Device, *structs.SemaphoreCreateInfo, *structs.AllocationCallbacks, *types.Semaphore) i32 = nil;
# loaded pointer for the device-level command vkDestroySemaphore
pub var vkDestroySemaphore: fun(types.Device, types.Semaphore, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkCreateQueryPool
pub var vkCreateQueryPool: fun(types.Device, *structs.QueryPoolCreateInfo, *structs.AllocationCallbacks, *types.QueryPool) i32 = nil;
# loaded pointer for the device-level command vkDestroyQueryPool
pub var vkDestroyQueryPool: fun(types.Device, types.QueryPool, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkGetQueryPoolResults
pub var vkGetQueryPoolResults: fun(types.Device, types.QueryPool, u32, u32, u64, ptr, u64, u32) i32 = nil;
# loaded pointer for the device-level command vkCreateBuffer
pub var vkCreateBuffer: fun(types.Device, *structs.BufferCreateInfo, *structs.AllocationCallbacks, *types.Buffer) i32 = nil;
# loaded pointer for the device-level command vkDestroyBuffer
pub var vkDestroyBuffer: fun(types.Device, types.Buffer, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkCreateImage
pub var vkCreateImage: fun(types.Device, *structs.ImageCreateInfo, *structs.AllocationCallbacks, *types.Image) i32 = nil;
# loaded pointer for the device-level command vkDestroyImage
pub var vkDestroyImage: fun(types.Device, types.Image, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkGetImageSubresourceLayout
pub var vkGetImageSubresourceLayout: fun(types.Device, types.Image, *structs.ImageSubresource, *structs.SubresourceLayout) = nil;
# loaded pointer for the device-level command vkCreateImageView
pub var vkCreateImageView: fun(types.Device, *structs.ImageViewCreateInfo, *structs.AllocationCallbacks, *types.ImageView) i32 = nil;
# loaded pointer for the device-level command vkDestroyImageView
pub var vkDestroyImageView: fun(types.Device, types.ImageView, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkCreateCommandPool
pub var vkCreateCommandPool: fun(types.Device, *structs.CommandPoolCreateInfo, *structs.AllocationCallbacks, *types.CommandPool) i32 = nil;
# loaded pointer for the device-level command vkDestroyCommandPool
pub var vkDestroyCommandPool: fun(types.Device, types.CommandPool, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkResetCommandPool
pub var vkResetCommandPool: fun(types.Device, types.CommandPool, u32) i32 = nil;
# loaded pointer for the device-level command vkAllocateCommandBuffers
pub var vkAllocateCommandBuffers: fun(types.Device, *structs.CommandBufferAllocateInfo, *types.CommandBuffer) i32 = nil;
# loaded pointer for the device-level command vkFreeCommandBuffers
pub var vkFreeCommandBuffers: fun(types.Device, types.CommandPool, u32, *types.CommandBuffer) = nil;
# loaded pointer for the device-level command vkBeginCommandBuffer
pub var vkBeginCommandBuffer: fun(types.CommandBuffer, *structs.CommandBufferBeginInfo) i32 = nil;
# loaded pointer for the device-level command vkEndCommandBuffer
pub var vkEndCommandBuffer: fun(types.CommandBuffer) i32 = nil;
# loaded pointer for the device-level command vkResetCommandBuffer
pub var vkResetCommandBuffer: fun(types.CommandBuffer, u32) i32 = nil;
# loaded pointer for the device-level command vkCmdCopyBuffer
pub var vkCmdCopyBuffer: fun(types.CommandBuffer, types.Buffer, types.Buffer, u32, *structs.BufferCopy) = nil;
# loaded pointer for the device-level command vkCmdCopyImage
pub var vkCmdCopyImage: fun(types.CommandBuffer, types.Image, i32, types.Image, i32, u32, *structs.ImageCopy) = nil;
# loaded pointer for the device-level command vkCmdCopyBufferToImage
pub var vkCmdCopyBufferToImage: fun(types.CommandBuffer, types.Buffer, types.Image, i32, u32, *structs.BufferImageCopy) = nil;
# loaded pointer for the device-level command vkCmdCopyImageToBuffer
pub var vkCmdCopyImageToBuffer: fun(types.CommandBuffer, types.Image, i32, types.Buffer, u32, *structs.BufferImageCopy) = nil;
# loaded pointer for the device-level command vkCmdUpdateBuffer
pub var vkCmdUpdateBuffer: fun(types.CommandBuffer, types.Buffer, u64, u64, ptr) = nil;
# loaded pointer for the device-level command vkCmdFillBuffer
pub var vkCmdFillBuffer: fun(types.CommandBuffer, types.Buffer, u64, u64, u32) = nil;
# loaded pointer for the device-level command vkCmdPipelineBarrier
pub var vkCmdPipelineBarrier: fun(types.CommandBuffer, u32, u32, u32, u32, *structs.MemoryBarrier, u32, *structs.BufferMemoryBarrier, u32, *structs.ImageMemoryBarrier) = nil;
# loaded pointer for the device-level command vkCmdBeginQuery
pub var vkCmdBeginQuery: fun(types.CommandBuffer, types.QueryPool, u32, u32) = nil;
# loaded pointer for the device-level command vkCmdEndQuery
pub var vkCmdEndQuery: fun(types.CommandBuffer, types.QueryPool, u32) = nil;
# loaded pointer for the device-level command vkCmdResetQueryPool
pub var vkCmdResetQueryPool: fun(types.CommandBuffer, types.QueryPool, u32, u32) = nil;
# loaded pointer for the device-level command vkCmdWriteTimestamp
pub var vkCmdWriteTimestamp: fun(types.CommandBuffer, i32, types.QueryPool, u32) = nil;
# loaded pointer for the device-level command vkCmdCopyQueryPoolResults
pub var vkCmdCopyQueryPoolResults: fun(types.CommandBuffer, types.QueryPool, u32, u32, types.Buffer, u64, u64, u32) = nil;
# loaded pointer for the device-level command vkCmdExecuteCommands
pub var vkCmdExecuteCommands: fun(types.CommandBuffer, u32, *types.CommandBuffer) = nil;
# loaded pointer for the device-level command vkCreateEvent
pub var vkCreateEvent: fun(types.Device, *structs.EventCreateInfo, *structs.AllocationCallbacks, *types.Event) i32 = nil;
# loaded pointer for the device-level command vkDestroyEvent
pub var vkDestroyEvent: fun(types.Device, types.Event, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkGetEventStatus
pub var vkGetEventStatus: fun(types.Device, types.Event) i32 = nil;
# loaded pointer for the device-level command vkSetEvent
pub var vkSetEvent: fun(types.Device, types.Event) i32 = nil;
# loaded pointer for the device-level command vkResetEvent
pub var vkResetEvent: fun(types.Device, types.Event) i32 = nil;
# loaded pointer for the device-level command vkCreateBufferView
pub var vkCreateBufferView: fun(types.Device, *structs.BufferViewCreateInfo, *structs.AllocationCallbacks, *types.BufferView) i32 = nil;
# loaded pointer for the device-level command vkDestroyBufferView
pub var vkDestroyBufferView: fun(types.Device, types.BufferView, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkCreateShaderModule
pub var vkCreateShaderModule: fun(types.Device, *structs.ShaderModuleCreateInfo, *structs.AllocationCallbacks, *types.ShaderModule) i32 = nil;
# loaded pointer for the device-level command vkDestroyShaderModule
pub var vkDestroyShaderModule: fun(types.Device, types.ShaderModule, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkCreatePipelineCache
pub var vkCreatePipelineCache: fun(types.Device, *structs.PipelineCacheCreateInfo, *structs.AllocationCallbacks, *types.PipelineCache) i32 = nil;
# loaded pointer for the device-level command vkDestroyPipelineCache
pub var vkDestroyPipelineCache: fun(types.Device, types.PipelineCache, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkGetPipelineCacheData
pub var vkGetPipelineCacheData: fun(types.Device, types.PipelineCache, *u64, ptr) i32 = nil;
# loaded pointer for the device-level command vkMergePipelineCaches
pub var vkMergePipelineCaches: fun(types.Device, types.PipelineCache, u32, *types.PipelineCache) i32 = nil;
# loaded pointer for the device-level command vkCreateComputePipelines
pub var vkCreateComputePipelines: fun(types.Device, types.PipelineCache, u32, *structs.ComputePipelineCreateInfo, *structs.AllocationCallbacks, *types.Pipeline) i32 = nil;
# loaded pointer for the device-level command vkDestroyPipeline
pub var vkDestroyPipeline: fun(types.Device, types.Pipeline, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkCreatePipelineLayout
pub var vkCreatePipelineLayout: fun(types.Device, *structs.PipelineLayoutCreateInfo, *structs.AllocationCallbacks, *types.PipelineLayout) i32 = nil;
# loaded pointer for the device-level command vkDestroyPipelineLayout
pub var vkDestroyPipelineLayout: fun(types.Device, types.PipelineLayout, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkCreateSampler
pub var vkCreateSampler: fun(types.Device, *structs.SamplerCreateInfo, *structs.AllocationCallbacks, *types.Sampler) i32 = nil;
# loaded pointer for the device-level command vkDestroySampler
pub var vkDestroySampler: fun(types.Device, types.Sampler, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkCreateDescriptorSetLayout
pub var vkCreateDescriptorSetLayout: fun(types.Device, *structs.DescriptorSetLayoutCreateInfo, *structs.AllocationCallbacks, *types.DescriptorSetLayout) i32 = nil;
# loaded pointer for the device-level command vkDestroyDescriptorSetLayout
pub var vkDestroyDescriptorSetLayout: fun(types.Device, types.DescriptorSetLayout, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkCreateDescriptorPool
pub var vkCreateDescriptorPool: fun(types.Device, *structs.DescriptorPoolCreateInfo, *structs.AllocationCallbacks, *types.DescriptorPool) i32 = nil;
# loaded pointer for the device-level command vkDestroyDescriptorPool
pub var vkDestroyDescriptorPool: fun(types.Device, types.DescriptorPool, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkResetDescriptorPool
pub var vkResetDescriptorPool: fun(types.Device, types.DescriptorPool, u32) i32 = nil;
# loaded pointer for the device-level command vkAllocateDescriptorSets
pub var vkAllocateDescriptorSets: fun(types.Device, *structs.DescriptorSetAllocateInfo, *types.DescriptorSet) i32 = nil;
# loaded pointer for the device-level command vkFreeDescriptorSets
pub var vkFreeDescriptorSets: fun(types.Device, types.DescriptorPool, u32, *types.DescriptorSet) i32 = nil;
# loaded pointer for the device-level command vkUpdateDescriptorSets
pub var vkUpdateDescriptorSets: fun(types.Device, u32, *structs.WriteDescriptorSet, u32, *structs.CopyDescriptorSet) = nil;
# loaded pointer for the device-level command vkCmdBindPipeline
pub var vkCmdBindPipeline: fun(types.CommandBuffer, i32, types.Pipeline) = nil;
# loaded pointer for the device-level command vkCmdBindDescriptorSets
pub var vkCmdBindDescriptorSets: fun(types.CommandBuffer, i32, types.PipelineLayout, u32, u32, *types.DescriptorSet, u32, *u32) = nil;
# loaded pointer for the device-level command vkCmdClearColorImage
pub var vkCmdClearColorImage: fun(types.CommandBuffer, types.Image, i32, *structs.ClearColorValue, u32, *structs.ImageSubresourceRange) = nil;
# loaded pointer for the device-level command vkCmdDispatch
pub var vkCmdDispatch: fun(types.CommandBuffer, u32, u32, u32) = nil;
# loaded pointer for the device-level command vkCmdDispatchIndirect
pub var vkCmdDispatchIndirect: fun(types.CommandBuffer, types.Buffer, u64) = nil;
# loaded pointer for the device-level command vkCmdSetEvent
pub var vkCmdSetEvent: fun(types.CommandBuffer, types.Event, u32) = nil;
# loaded pointer for the device-level command vkCmdResetEvent
pub var vkCmdResetEvent: fun(types.CommandBuffer, types.Event, u32) = nil;
# loaded pointer for the device-level command vkCmdWaitEvents
pub var vkCmdWaitEvents: fun(types.CommandBuffer, u32, *types.Event, u32, u32, u32, *structs.MemoryBarrier, u32, *structs.BufferMemoryBarrier, u32, *structs.ImageMemoryBarrier) = nil;
# loaded pointer for the device-level command vkCmdPushConstants
pub var vkCmdPushConstants: fun(types.CommandBuffer, types.PipelineLayout, u32, u32, u32, ptr) = nil;
# loaded pointer for the device-level command vkCreateGraphicsPipelines
pub var vkCreateGraphicsPipelines: fun(types.Device, types.PipelineCache, u32, *structs.GraphicsPipelineCreateInfo, *structs.AllocationCallbacks, *types.Pipeline) i32 = nil;
# loaded pointer for the device-level command vkCreateFramebuffer
pub var vkCreateFramebuffer: fun(types.Device, *structs.FramebufferCreateInfo, *structs.AllocationCallbacks, *types.Framebuffer) i32 = nil;
# loaded pointer for the device-level command vkDestroyFramebuffer
pub var vkDestroyFramebuffer: fun(types.Device, types.Framebuffer, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkCreateRenderPass
pub var vkCreateRenderPass: fun(types.Device, *structs.RenderPassCreateInfo, *structs.AllocationCallbacks, *types.RenderPass) i32 = nil;
# loaded pointer for the device-level command vkDestroyRenderPass
pub var vkDestroyRenderPass: fun(types.Device, types.RenderPass, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkGetRenderAreaGranularity
pub var vkGetRenderAreaGranularity: fun(types.Device, types.RenderPass, *structs.Extent2D) = nil;
# loaded pointer for the device-level command vkCmdSetViewport
pub var vkCmdSetViewport: fun(types.CommandBuffer, u32, u32, *structs.Viewport) = nil;
# loaded pointer for the device-level command vkCmdSetScissor
pub var vkCmdSetScissor: fun(types.CommandBuffer, u32, u32, *structs.Rect2D) = nil;
# loaded pointer for the device-level command vkCmdSetLineWidth
pub var vkCmdSetLineWidth: fun(types.CommandBuffer, f32) = nil;
# loaded pointer for the device-level command vkCmdSetDepthBias
pub var vkCmdSetDepthBias: fun(types.CommandBuffer, f32, f32, f32) = nil;
# loaded pointer for the device-level command vkCmdSetBlendConstants
pub var vkCmdSetBlendConstants: fun(types.CommandBuffer, [4]f32) = nil;
# loaded pointer for the device-level command vkCmdSetDepthBounds
pub var vkCmdSetDepthBounds: fun(types.CommandBuffer, f32, f32) = nil;
# loaded pointer for the device-level command vkCmdSetStencilCompareMask
pub var vkCmdSetStencilCompareMask: fun(types.CommandBuffer, u32, u32) = nil;
# loaded pointer for the device-level command vkCmdSetStencilWriteMask
pub var vkCmdSetStencilWriteMask: fun(types.CommandBuffer, u32, u32) = nil;
# loaded pointer for the device-level command vkCmdSetStencilReference
pub var vkCmdSetStencilReference: fun(types.CommandBuffer, u32, u32) = nil;
# loaded pointer for the device-level command vkCmdBindIndexBuffer
pub var vkCmdBindIndexBuffer: fun(types.CommandBuffer, types.Buffer, u64, i32) = nil;
# loaded pointer for the device-level command vkCmdBindVertexBuffers
pub var vkCmdBindVertexBuffers: fun(types.CommandBuffer, u32, u32, *types.Buffer, *u64) = nil;
# loaded pointer for the device-level command vkCmdDraw
pub var vkCmdDraw: fun(types.CommandBuffer, u32, u32, u32, u32) = nil;
# loaded pointer for the device-level command vkCmdDrawIndexed
pub var vkCmdDrawIndexed: fun(types.CommandBuffer, u32, u32, u32, i32, u32) = nil;
# loaded pointer for the device-level command vkCmdDrawIndirect
pub var vkCmdDrawIndirect: fun(types.CommandBuffer, types.Buffer, u64, u32, u32) = nil;
# loaded pointer for the device-level command vkCmdDrawIndexedIndirect
pub var vkCmdDrawIndexedIndirect: fun(types.CommandBuffer, types.Buffer, u64, u32, u32) = nil;
# loaded pointer for the device-level command vkCmdBlitImage
pub var vkCmdBlitImage: fun(types.CommandBuffer, types.Image, i32, types.Image, i32, u32, *structs.ImageBlit, i32) = nil;
# loaded pointer for the device-level command vkCmdClearDepthStencilImage
pub var vkCmdClearDepthStencilImage: fun(types.CommandBuffer, types.Image, i32, *structs.ClearDepthStencilValue, u32, *structs.ImageSubresourceRange) = nil;
# loaded pointer for the device-level command vkCmdClearAttachments
pub var vkCmdClearAttachments: fun(types.CommandBuffer, u32, *structs.ClearAttachment, u32, *structs.ClearRect) = nil;
# loaded pointer for the device-level command vkCmdResolveImage
pub var vkCmdResolveImage: fun(types.CommandBuffer, types.Image, i32, types.Image, i32, u32, *structs.ImageResolve) = nil;
# loaded pointer for the device-level command vkCmdBeginRenderPass
pub var vkCmdBeginRenderPass: fun(types.CommandBuffer, *structs.RenderPassBeginInfo, i32) = nil;
# loaded pointer for the device-level command vkCmdNextSubpass
pub var vkCmdNextSubpass: fun(types.CommandBuffer, i32) = nil;
# loaded pointer for the device-level command vkCmdEndRenderPass
pub var vkCmdEndRenderPass: fun(types.CommandBuffer) = nil;
# loaded pointer for the global-level command vkEnumerateInstanceVersion
pub var vkEnumerateInstanceVersion: fun(*u32) i32 = nil;
# loaded pointer for the device-level command vkBindBufferMemory2
pub var vkBindBufferMemory2: fun(types.Device, u32, *structs.BindBufferMemoryInfo) i32 = nil;
# loaded pointer for the device-level command vkBindImageMemory2
pub var vkBindImageMemory2: fun(types.Device, u32, *structs.BindImageMemoryInfo) i32 = nil;
# loaded pointer for the device-level command vkGetDeviceGroupPeerMemoryFeatures
pub var vkGetDeviceGroupPeerMemoryFeatures: fun(types.Device, u32, u32, u32, *u32) = nil;
# loaded pointer for the device-level command vkCmdSetDeviceMask
pub var vkCmdSetDeviceMask: fun(types.CommandBuffer, u32) = nil;
# loaded pointer for the instance-level command vkEnumeratePhysicalDeviceGroups
pub var vkEnumeratePhysicalDeviceGroups: fun(types.Instance, *u32, *structs.PhysicalDeviceGroupProperties) i32 = nil;
# loaded pointer for the device-level command vkGetImageMemoryRequirements2
pub var vkGetImageMemoryRequirements2: fun(types.Device, *structs.ImageMemoryRequirementsInfo2, *structs.MemoryRequirements2) = nil;
# loaded pointer for the device-level command vkGetBufferMemoryRequirements2
pub var vkGetBufferMemoryRequirements2: fun(types.Device, *structs.BufferMemoryRequirementsInfo2, *structs.MemoryRequirements2) = nil;
# loaded pointer for the device-level command vkGetImageSparseMemoryRequirements2
pub var vkGetImageSparseMemoryRequirements2: fun(types.Device, *structs.ImageSparseMemoryRequirementsInfo2, *u32, *structs.SparseImageMemoryRequirements2) = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceFeatures2
pub var vkGetPhysicalDeviceFeatures2: fun(types.PhysicalDevice, *structs.PhysicalDeviceFeatures2) = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceProperties2
pub var vkGetPhysicalDeviceProperties2: fun(types.PhysicalDevice, *structs.PhysicalDeviceProperties2) = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceFormatProperties2
pub var vkGetPhysicalDeviceFormatProperties2: fun(types.PhysicalDevice, i32, *structs.FormatProperties2) = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceImageFormatProperties2
pub var vkGetPhysicalDeviceImageFormatProperties2: fun(types.PhysicalDevice, *structs.PhysicalDeviceImageFormatInfo2, *structs.ImageFormatProperties2) i32 = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceQueueFamilyProperties2
pub var vkGetPhysicalDeviceQueueFamilyProperties2: fun(types.PhysicalDevice, *u32, *structs.QueueFamilyProperties2) = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceMemoryProperties2
pub var vkGetPhysicalDeviceMemoryProperties2: fun(types.PhysicalDevice, *structs.PhysicalDeviceMemoryProperties2) = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceSparseImageFormatProperties2
pub var vkGetPhysicalDeviceSparseImageFormatProperties2: fun(types.PhysicalDevice, *structs.PhysicalDeviceSparseImageFormatInfo2, *u32, *structs.SparseImageFormatProperties2) = nil;
# loaded pointer for the device-level command vkTrimCommandPool
pub var vkTrimCommandPool: fun(types.Device, types.CommandPool, u32) = nil;
# loaded pointer for the device-level command vkGetDeviceQueue2
pub var vkGetDeviceQueue2: fun(types.Device, *structs.DeviceQueueInfo2, *types.Queue) = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceExternalBufferProperties
pub var vkGetPhysicalDeviceExternalBufferProperties: fun(types.PhysicalDevice, *structs.PhysicalDeviceExternalBufferInfo, *structs.ExternalBufferProperties) = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceExternalFenceProperties
pub var vkGetPhysicalDeviceExternalFenceProperties: fun(types.PhysicalDevice, *structs.PhysicalDeviceExternalFenceInfo, *structs.ExternalFenceProperties) = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceExternalSemaphoreProperties
pub var vkGetPhysicalDeviceExternalSemaphoreProperties: fun(types.PhysicalDevice, *structs.PhysicalDeviceExternalSemaphoreInfo, *structs.ExternalSemaphoreProperties) = nil;
# loaded pointer for the device-level command vkCmdDispatchBase
pub var vkCmdDispatchBase: fun(types.CommandBuffer, u32, u32, u32, u32, u32, u32) = nil;
# loaded pointer for the device-level command vkCreateDescriptorUpdateTemplate
pub var vkCreateDescriptorUpdateTemplate: fun(types.Device, *structs.DescriptorUpdateTemplateCreateInfo, *structs.AllocationCallbacks, *types.DescriptorUpdateTemplate) i32 = nil;
# loaded pointer for the device-level command vkDestroyDescriptorUpdateTemplate
pub var vkDestroyDescriptorUpdateTemplate: fun(types.Device, types.DescriptorUpdateTemplate, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkUpdateDescriptorSetWithTemplate
pub var vkUpdateDescriptorSetWithTemplate: fun(types.Device, types.DescriptorSet, types.DescriptorUpdateTemplate, ptr) = nil;
# loaded pointer for the device-level command vkGetDescriptorSetLayoutSupport
pub var vkGetDescriptorSetLayoutSupport: fun(types.Device, *structs.DescriptorSetLayoutCreateInfo, *structs.DescriptorSetLayoutSupport) = nil;
# loaded pointer for the device-level command vkCreateSamplerYcbcrConversion
pub var vkCreateSamplerYcbcrConversion: fun(types.Device, *structs.SamplerYcbcrConversionCreateInfo, *structs.AllocationCallbacks, *types.SamplerYcbcrConversion) i32 = nil;
# loaded pointer for the device-level command vkDestroySamplerYcbcrConversion
pub var vkDestroySamplerYcbcrConversion: fun(types.Device, types.SamplerYcbcrConversion, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkResetQueryPool
pub var vkResetQueryPool: fun(types.Device, types.QueryPool, u32, u32) = nil;
# loaded pointer for the device-level command vkGetSemaphoreCounterValue
pub var vkGetSemaphoreCounterValue: fun(types.Device, types.Semaphore, *u64) i32 = nil;
# loaded pointer for the device-level command vkWaitSemaphores
pub var vkWaitSemaphores: fun(types.Device, *structs.SemaphoreWaitInfo, u64) i32 = nil;
# loaded pointer for the device-level command vkSignalSemaphore
pub var vkSignalSemaphore: fun(types.Device, *structs.SemaphoreSignalInfo) i32 = nil;
# loaded pointer for the device-level command vkGetBufferDeviceAddress
pub var vkGetBufferDeviceAddress: fun(types.Device, *structs.BufferDeviceAddressInfo) u64 = nil;
# loaded pointer for the device-level command vkGetBufferOpaqueCaptureAddress
pub var vkGetBufferOpaqueCaptureAddress: fun(types.Device, *structs.BufferDeviceAddressInfo) u64 = nil;
# loaded pointer for the device-level command vkGetDeviceMemoryOpaqueCaptureAddress
pub var vkGetDeviceMemoryOpaqueCaptureAddress: fun(types.Device, *structs.DeviceMemoryOpaqueCaptureAddressInfo) u64 = nil;
# loaded pointer for the device-level command vkCmdDrawIndirectCount
pub var vkCmdDrawIndirectCount: fun(types.CommandBuffer, types.Buffer, u64, types.Buffer, u64, u32, u32) = nil;
# loaded pointer for the device-level command vkCmdDrawIndexedIndirectCount
pub var vkCmdDrawIndexedIndirectCount: fun(types.CommandBuffer, types.Buffer, u64, types.Buffer, u64, u32, u32) = nil;
# loaded pointer for the device-level command vkCreateRenderPass2
pub var vkCreateRenderPass2: fun(types.Device, *structs.RenderPassCreateInfo2, *structs.AllocationCallbacks, *types.RenderPass) i32 = nil;
# loaded pointer for the device-level command vkCmdBeginRenderPass2
pub var vkCmdBeginRenderPass2: fun(types.CommandBuffer, *structs.RenderPassBeginInfo, *structs.SubpassBeginInfo) = nil;
# loaded pointer for the device-level command vkCmdNextSubpass2
pub var vkCmdNextSubpass2: fun(types.CommandBuffer, *structs.SubpassBeginInfo, *structs.SubpassEndInfo) = nil;
# loaded pointer for the device-level command vkCmdEndRenderPass2
pub var vkCmdEndRenderPass2: fun(types.CommandBuffer, *structs.SubpassEndInfo) = nil;
# loaded pointer for the instance-level command vkGetPhysicalDeviceToolProperties
pub var vkGetPhysicalDeviceToolProperties: fun(types.PhysicalDevice, *u32, *structs.PhysicalDeviceToolProperties) i32 = nil;
# loaded pointer for the device-level command vkCreatePrivateDataSlot
pub var vkCreatePrivateDataSlot: fun(types.Device, *structs.PrivateDataSlotCreateInfo, *structs.AllocationCallbacks, *types.PrivateDataSlot) i32 = nil;
# loaded pointer for the device-level command vkDestroyPrivateDataSlot
pub var vkDestroyPrivateDataSlot: fun(types.Device, types.PrivateDataSlot, *structs.AllocationCallbacks) = nil;
# loaded pointer for the device-level command vkSetPrivateData
pub var vkSetPrivateData: fun(types.Device, i32, u64, types.PrivateDataSlot, u64) i32 = nil;
# loaded pointer for the device-level command vkGetPrivateData
pub var vkGetPrivateData: fun(types.Device, i32, u64, types.PrivateDataSlot, *u64) = nil;
# loaded pointer for the device-level command vkCmdPipelineBarrier2
pub var vkCmdPipelineBarrier2: fun(types.CommandBuffer, *structs.DependencyInfo) = nil;
# loaded pointer for the device-level command vkCmdWriteTimestamp2
pub var vkCmdWriteTimestamp2: fun(types.CommandBuffer, u64, types.QueryPool, u32) = nil;
# loaded pointer for the device-level command vkQueueSubmit2
pub var vkQueueSubmit2: fun(types.Queue, u32, *structs.SubmitInfo2, types.Fence) i32 = nil;
# loaded pointer for the device-level command vkCmdCopyBuffer2
pub var vkCmdCopyBuffer2: fun(types.CommandBuffer, *structs.CopyBufferInfo2) = nil;
# loaded pointer for the device-level command vkCmdCopyImage2
pub var vkCmdCopyImage2: fun(types.CommandBuffer, *structs.CopyImageInfo2) = nil;
# loaded pointer for the device-level command vkCmdCopyBufferToImage2
pub var vkCmdCopyBufferToImage2: fun(types.CommandBuffer, *structs.CopyBufferToImageInfo2) = nil;
# loaded pointer for the device-level command vkCmdCopyImageToBuffer2
pub var vkCmdCopyImageToBuffer2: fun(types.CommandBuffer, *structs.CopyImageToBufferInfo2) = nil;
# loaded pointer for the device-level command vkGetDeviceBufferMemoryRequirements
pub var vkGetDeviceBufferMemoryRequirements: fun(types.Device, *structs.DeviceBufferMemoryRequirements, *structs.MemoryRequirements2) = nil;
# loaded pointer for the device-level command vkGetDeviceImageMemoryRequirements
pub var vkGetDeviceImageMemoryRequirements: fun(types.Device, *structs.DeviceImageMemoryRequirements, *structs.MemoryRequirements2) = nil;
# loaded pointer for the device-level command vkGetDeviceImageSparseMemoryRequirements
pub var vkGetDeviceImageSparseMemoryRequirements: fun(types.Device, *structs.DeviceImageMemoryRequirements, *u32, *structs.SparseImageMemoryRequirements2) = nil;
# loaded pointer for the device-level command vkCmdSetEvent2
pub var vkCmdSetEvent2: fun(types.CommandBuffer, types.Event, *structs.DependencyInfo) = nil;
# loaded pointer for the device-level command vkCmdResetEvent2
pub var vkCmdResetEvent2: fun(types.CommandBuffer, types.Event, u64) = nil;
# loaded pointer for the device-level command vkCmdWaitEvents2
pub var vkCmdWaitEvents2: fun(types.CommandBuffer, u32, *types.Event, *structs.DependencyInfo) = nil;
# loaded pointer for the device-level command vkCmdBlitImage2
pub var vkCmdBlitImage2: fun(types.CommandBuffer, *structs.BlitImageInfo2) = nil;
# loaded pointer for the device-level command vkCmdResolveImage2
pub var vkCmdResolveImage2: fun(types.CommandBuffer, *structs.ResolveImageInfo2) = nil;
# loaded pointer for the device-level command vkCmdBeginRendering
pub var vkCmdBeginRendering: fun(types.CommandBuffer, *structs.RenderingInfo) = nil;
# loaded pointer for the device-level command vkCmdEndRendering
pub var vkCmdEndRendering: fun(types.CommandBuffer) = nil;
# loaded pointer for the device-level command vkCmdSetCullMode
pub var vkCmdSetCullMode: fun(types.CommandBuffer, u32) = nil;
# loaded pointer for the device-level command vkCmdSetFrontFace
pub var vkCmdSetFrontFace: fun(types.CommandBuffer, i32) = nil;
# loaded pointer for the device-level command vkCmdSetPrimitiveTopology
pub var vkCmdSetPrimitiveTopology: fun(types.CommandBuffer, i32) = nil;
# loaded pointer for the device-level command vkCmdSetViewportWithCount
pub var vkCmdSetViewportWithCount: fun(types.CommandBuffer, u32, *structs.Viewport) = nil;
# loaded pointer for the device-level command vkCmdSetScissorWithCount
pub var vkCmdSetScissorWithCount: fun(types.CommandBuffer, u32, *structs.Rect2D) = nil;
# loaded pointer for the device-level command vkCmdBindVertexBuffers2
pub var vkCmdBindVertexBuffers2: fun(types.CommandBuffer, u32, u32, *types.Buffer, *u64, *u64, *u64) = nil;
# loaded pointer for the device-level command vkCmdSetDepthTestEnable
pub var vkCmdSetDepthTestEnable: fun(types.CommandBuffer, u32) = nil;
# loaded pointer for the device-level command vkCmdSetDepthWriteEnable
pub var vkCmdSetDepthWriteEnable: fun(types.CommandBuffer, u32) = nil;
# loaded pointer for the device-level command vkCmdSetDepthCompareOp
pub var vkCmdSetDepthCompareOp: fun(types.CommandBuffer, i32) = nil;
# loaded pointer for the device-level command vkCmdSetDepthBoundsTestEnable
pub var vkCmdSetDepthBoundsTestEnable: fun(types.CommandBuffer, u32) = nil;
# loaded pointer for the device-level command vkCmdSetStencilTestEnable
pub var vkCmdSetStencilTestEnable: fun(types.CommandBuffer, u32) = nil;
# loaded pointer for the device-level command vkCmdSetStencilOp
pub var vkCmdSetStencilOp: fun(types.CommandBuffer, u32, i32, i32, i32, i32) = nil;
# loaded pointer for the device-level command vkCmdSetRasterizerDiscardEnable
pub var vkCmdSetRasterizerDiscardEnable: fun(types.CommandBuffer, u32) = nil;
# loaded pointer for the device-level command vkCmdSetDepthBiasEnable
pub var vkCmdSetDepthBiasEnable: fun(types.CommandBuffer, u32) = nil;
# loaded pointer for the device-level command vkCmdSetPrimitiveRestartEnable
pub var vkCmdSetPrimitiveRestartEnable: fun(types.CommandBuffer, u32) = nil;

# resolve the instanceless global commands through the caller's bootstrap loader, passing a nil instance
# ---
# gipa: the bootstrap vkGetInstanceProcAddr
# ret : the number of global commands resolved; unresolved pointers stay nil
pub fun load_global(gipa: GetInstanceProcAddr) i64 {
    var n: i64 = 0;
    vkCreateInstance = gipa(nil, "vkCreateInstance"):~fun(*structs.InstanceCreateInfo, *structs.AllocationCallbacks, *types.Instance) i32;
    if (vkCreateInstance != nil) { n = n + 1; }
    vkEnumerateInstanceExtensionProperties = gipa(nil, "vkEnumerateInstanceExtensionProperties"):~fun(*u8, *u32, *structs.ExtensionProperties) i32;
    if (vkEnumerateInstanceExtensionProperties != nil) { n = n + 1; }
    vkEnumerateInstanceLayerProperties = gipa(nil, "vkEnumerateInstanceLayerProperties"):~fun(*u32, *structs.LayerProperties) i32;
    if (vkEnumerateInstanceLayerProperties != nil) { n = n + 1; }
    vkEnumerateInstanceVersion = gipa(nil, "vkEnumerateInstanceVersion"):~fun(*u32) i32;
    if (vkEnumerateInstanceVersion != nil) { n = n + 1; }
    ret n;
}

# resolve the instance-level commands through the caller's bootstrap loader against a created instance
# ---
# gipa    : the bootstrap vkGetInstanceProcAddr
# instance: the created instance to resolve against
# ret     : the number of instance commands resolved; unresolved pointers stay nil
pub fun load_instance(gipa: GetInstanceProcAddr, instance: ptr) i64 {
    var n: i64 = 0;
    vkDestroyInstance = gipa(instance, "vkDestroyInstance"):~fun(types.Instance, *structs.AllocationCallbacks);
    if (vkDestroyInstance != nil) { n = n + 1; }
    vkEnumeratePhysicalDevices = gipa(instance, "vkEnumeratePhysicalDevices"):~fun(types.Instance, *u32, *types.PhysicalDevice) i32;
    if (vkEnumeratePhysicalDevices != nil) { n = n + 1; }
    vkGetPhysicalDeviceFeatures = gipa(instance, "vkGetPhysicalDeviceFeatures"):~fun(types.PhysicalDevice, *structs.PhysicalDeviceFeatures);
    if (vkGetPhysicalDeviceFeatures != nil) { n = n + 1; }
    vkGetPhysicalDeviceFormatProperties = gipa(instance, "vkGetPhysicalDeviceFormatProperties"):~fun(types.PhysicalDevice, i32, *structs.FormatProperties);
    if (vkGetPhysicalDeviceFormatProperties != nil) { n = n + 1; }
    vkGetPhysicalDeviceImageFormatProperties = gipa(instance, "vkGetPhysicalDeviceImageFormatProperties"):~fun(types.PhysicalDevice, i32, i32, i32, u32, u32, *structs.ImageFormatProperties) i32;
    if (vkGetPhysicalDeviceImageFormatProperties != nil) { n = n + 1; }
    vkGetPhysicalDeviceProperties = gipa(instance, "vkGetPhysicalDeviceProperties"):~fun(types.PhysicalDevice, *structs.PhysicalDeviceProperties);
    if (vkGetPhysicalDeviceProperties != nil) { n = n + 1; }
    vkGetPhysicalDeviceQueueFamilyProperties = gipa(instance, "vkGetPhysicalDeviceQueueFamilyProperties"):~fun(types.PhysicalDevice, *u32, *structs.QueueFamilyProperties);
    if (vkGetPhysicalDeviceQueueFamilyProperties != nil) { n = n + 1; }
    vkGetPhysicalDeviceMemoryProperties = gipa(instance, "vkGetPhysicalDeviceMemoryProperties"):~fun(types.PhysicalDevice, *structs.PhysicalDeviceMemoryProperties);
    if (vkGetPhysicalDeviceMemoryProperties != nil) { n = n + 1; }
    vkGetInstanceProcAddr = gipa(instance, "vkGetInstanceProcAddr"):~fun(types.Instance, *u8) types.VoidFunction;
    if (vkGetInstanceProcAddr != nil) { n = n + 1; }
    vkCreateDevice = gipa(instance, "vkCreateDevice"):~fun(types.PhysicalDevice, *structs.DeviceCreateInfo, *structs.AllocationCallbacks, *types.Device) i32;
    if (vkCreateDevice != nil) { n = n + 1; }
    vkEnumerateDeviceExtensionProperties = gipa(instance, "vkEnumerateDeviceExtensionProperties"):~fun(types.PhysicalDevice, *u8, *u32, *structs.ExtensionProperties) i32;
    if (vkEnumerateDeviceExtensionProperties != nil) { n = n + 1; }
    vkEnumerateDeviceLayerProperties = gipa(instance, "vkEnumerateDeviceLayerProperties"):~fun(types.PhysicalDevice, *u32, *structs.LayerProperties) i32;
    if (vkEnumerateDeviceLayerProperties != nil) { n = n + 1; }
    vkGetPhysicalDeviceSparseImageFormatProperties = gipa(instance, "vkGetPhysicalDeviceSparseImageFormatProperties"):~fun(types.PhysicalDevice, i32, i32, i32, u32, i32, *u32, *structs.SparseImageFormatProperties);
    if (vkGetPhysicalDeviceSparseImageFormatProperties != nil) { n = n + 1; }
    vkEnumeratePhysicalDeviceGroups = gipa(instance, "vkEnumeratePhysicalDeviceGroups"):~fun(types.Instance, *u32, *structs.PhysicalDeviceGroupProperties) i32;
    if (vkEnumeratePhysicalDeviceGroups != nil) { n = n + 1; }
    vkGetPhysicalDeviceFeatures2 = gipa(instance, "vkGetPhysicalDeviceFeatures2"):~fun(types.PhysicalDevice, *structs.PhysicalDeviceFeatures2);
    if (vkGetPhysicalDeviceFeatures2 != nil) { n = n + 1; }
    vkGetPhysicalDeviceProperties2 = gipa(instance, "vkGetPhysicalDeviceProperties2"):~fun(types.PhysicalDevice, *structs.PhysicalDeviceProperties2);
    if (vkGetPhysicalDeviceProperties2 != nil) { n = n + 1; }
    vkGetPhysicalDeviceFormatProperties2 = gipa(instance, "vkGetPhysicalDeviceFormatProperties2"):~fun(types.PhysicalDevice, i32, *structs.FormatProperties2);
    if (vkGetPhysicalDeviceFormatProperties2 != nil) { n = n + 1; }
    vkGetPhysicalDeviceImageFormatProperties2 = gipa(instance, "vkGetPhysicalDeviceImageFormatProperties2"):~fun(types.PhysicalDevice, *structs.PhysicalDeviceImageFormatInfo2, *structs.ImageFormatProperties2) i32;
    if (vkGetPhysicalDeviceImageFormatProperties2 != nil) { n = n + 1; }
    vkGetPhysicalDeviceQueueFamilyProperties2 = gipa(instance, "vkGetPhysicalDeviceQueueFamilyProperties2"):~fun(types.PhysicalDevice, *u32, *structs.QueueFamilyProperties2);
    if (vkGetPhysicalDeviceQueueFamilyProperties2 != nil) { n = n + 1; }
    vkGetPhysicalDeviceMemoryProperties2 = gipa(instance, "vkGetPhysicalDeviceMemoryProperties2"):~fun(types.PhysicalDevice, *structs.PhysicalDeviceMemoryProperties2);
    if (vkGetPhysicalDeviceMemoryProperties2 != nil) { n = n + 1; }
    vkGetPhysicalDeviceSparseImageFormatProperties2 = gipa(instance, "vkGetPhysicalDeviceSparseImageFormatProperties2"):~fun(types.PhysicalDevice, *structs.PhysicalDeviceSparseImageFormatInfo2, *u32, *structs.SparseImageFormatProperties2);
    if (vkGetPhysicalDeviceSparseImageFormatProperties2 != nil) { n = n + 1; }
    vkGetPhysicalDeviceExternalBufferProperties = gipa(instance, "vkGetPhysicalDeviceExternalBufferProperties"):~fun(types.PhysicalDevice, *structs.PhysicalDeviceExternalBufferInfo, *structs.ExternalBufferProperties);
    if (vkGetPhysicalDeviceExternalBufferProperties != nil) { n = n + 1; }
    vkGetPhysicalDeviceExternalFenceProperties = gipa(instance, "vkGetPhysicalDeviceExternalFenceProperties"):~fun(types.PhysicalDevice, *structs.PhysicalDeviceExternalFenceInfo, *structs.ExternalFenceProperties);
    if (vkGetPhysicalDeviceExternalFenceProperties != nil) { n = n + 1; }
    vkGetPhysicalDeviceExternalSemaphoreProperties = gipa(instance, "vkGetPhysicalDeviceExternalSemaphoreProperties"):~fun(types.PhysicalDevice, *structs.PhysicalDeviceExternalSemaphoreInfo, *structs.ExternalSemaphoreProperties);
    if (vkGetPhysicalDeviceExternalSemaphoreProperties != nil) { n = n + 1; }
    vkGetPhysicalDeviceToolProperties = gipa(instance, "vkGetPhysicalDeviceToolProperties"):~fun(types.PhysicalDevice, *u32, *structs.PhysicalDeviceToolProperties) i32;
    if (vkGetPhysicalDeviceToolProperties != nil) { n = n + 1; }
    ret n;
}

# resolve the device-level commands through vkGetDeviceProcAddr, skipping the instance dispatch trampoline
# ---
# gdpa  : the device's vkGetDeviceProcAddr
# device: the created device to resolve against
# ret   : the number of device commands resolved; unresolved pointers stay nil
pub fun load_device(gdpa: GetDeviceProcAddr, device: ptr) i64 {
    var n: i64 = 0;
    vkGetDeviceProcAddr = gdpa(device, "vkGetDeviceProcAddr"):~fun(types.Device, *u8) types.VoidFunction;
    if (vkGetDeviceProcAddr != nil) { n = n + 1; }
    vkDestroyDevice = gdpa(device, "vkDestroyDevice"):~fun(types.Device, *structs.AllocationCallbacks);
    if (vkDestroyDevice != nil) { n = n + 1; }
    vkGetDeviceQueue = gdpa(device, "vkGetDeviceQueue"):~fun(types.Device, u32, u32, *types.Queue);
    if (vkGetDeviceQueue != nil) { n = n + 1; }
    vkQueueSubmit = gdpa(device, "vkQueueSubmit"):~fun(types.Queue, u32, *structs.SubmitInfo, types.Fence) i32;
    if (vkQueueSubmit != nil) { n = n + 1; }
    vkQueueWaitIdle = gdpa(device, "vkQueueWaitIdle"):~fun(types.Queue) i32;
    if (vkQueueWaitIdle != nil) { n = n + 1; }
    vkDeviceWaitIdle = gdpa(device, "vkDeviceWaitIdle"):~fun(types.Device) i32;
    if (vkDeviceWaitIdle != nil) { n = n + 1; }
    vkAllocateMemory = gdpa(device, "vkAllocateMemory"):~fun(types.Device, *structs.MemoryAllocateInfo, *structs.AllocationCallbacks, *types.DeviceMemory) i32;
    if (vkAllocateMemory != nil) { n = n + 1; }
    vkFreeMemory = gdpa(device, "vkFreeMemory"):~fun(types.Device, types.DeviceMemory, *structs.AllocationCallbacks);
    if (vkFreeMemory != nil) { n = n + 1; }
    vkMapMemory = gdpa(device, "vkMapMemory"):~fun(types.Device, types.DeviceMemory, u64, u64, u32, *ptr) i32;
    if (vkMapMemory != nil) { n = n + 1; }
    vkUnmapMemory = gdpa(device, "vkUnmapMemory"):~fun(types.Device, types.DeviceMemory);
    if (vkUnmapMemory != nil) { n = n + 1; }
    vkFlushMappedMemoryRanges = gdpa(device, "vkFlushMappedMemoryRanges"):~fun(types.Device, u32, *structs.MappedMemoryRange) i32;
    if (vkFlushMappedMemoryRanges != nil) { n = n + 1; }
    vkInvalidateMappedMemoryRanges = gdpa(device, "vkInvalidateMappedMemoryRanges"):~fun(types.Device, u32, *structs.MappedMemoryRange) i32;
    if (vkInvalidateMappedMemoryRanges != nil) { n = n + 1; }
    vkGetDeviceMemoryCommitment = gdpa(device, "vkGetDeviceMemoryCommitment"):~fun(types.Device, types.DeviceMemory, *u64);
    if (vkGetDeviceMemoryCommitment != nil) { n = n + 1; }
    vkBindBufferMemory = gdpa(device, "vkBindBufferMemory"):~fun(types.Device, types.Buffer, types.DeviceMemory, u64) i32;
    if (vkBindBufferMemory != nil) { n = n + 1; }
    vkBindImageMemory = gdpa(device, "vkBindImageMemory"):~fun(types.Device, types.Image, types.DeviceMemory, u64) i32;
    if (vkBindImageMemory != nil) { n = n + 1; }
    vkGetBufferMemoryRequirements = gdpa(device, "vkGetBufferMemoryRequirements"):~fun(types.Device, types.Buffer, *structs.MemoryRequirements);
    if (vkGetBufferMemoryRequirements != nil) { n = n + 1; }
    vkGetImageMemoryRequirements = gdpa(device, "vkGetImageMemoryRequirements"):~fun(types.Device, types.Image, *structs.MemoryRequirements);
    if (vkGetImageMemoryRequirements != nil) { n = n + 1; }
    vkGetImageSparseMemoryRequirements = gdpa(device, "vkGetImageSparseMemoryRequirements"):~fun(types.Device, types.Image, *u32, *structs.SparseImageMemoryRequirements);
    if (vkGetImageSparseMemoryRequirements != nil) { n = n + 1; }
    vkQueueBindSparse = gdpa(device, "vkQueueBindSparse"):~fun(types.Queue, u32, *structs.BindSparseInfo, types.Fence) i32;
    if (vkQueueBindSparse != nil) { n = n + 1; }
    vkCreateFence = gdpa(device, "vkCreateFence"):~fun(types.Device, *structs.FenceCreateInfo, *structs.AllocationCallbacks, *types.Fence) i32;
    if (vkCreateFence != nil) { n = n + 1; }
    vkDestroyFence = gdpa(device, "vkDestroyFence"):~fun(types.Device, types.Fence, *structs.AllocationCallbacks);
    if (vkDestroyFence != nil) { n = n + 1; }
    vkResetFences = gdpa(device, "vkResetFences"):~fun(types.Device, u32, *types.Fence) i32;
    if (vkResetFences != nil) { n = n + 1; }
    vkGetFenceStatus = gdpa(device, "vkGetFenceStatus"):~fun(types.Device, types.Fence) i32;
    if (vkGetFenceStatus != nil) { n = n + 1; }
    vkWaitForFences = gdpa(device, "vkWaitForFences"):~fun(types.Device, u32, *types.Fence, u32, u64) i32;
    if (vkWaitForFences != nil) { n = n + 1; }
    vkCreateSemaphore = gdpa(device, "vkCreateSemaphore"):~fun(types.Device, *structs.SemaphoreCreateInfo, *structs.AllocationCallbacks, *types.Semaphore) i32;
    if (vkCreateSemaphore != nil) { n = n + 1; }
    vkDestroySemaphore = gdpa(device, "vkDestroySemaphore"):~fun(types.Device, types.Semaphore, *structs.AllocationCallbacks);
    if (vkDestroySemaphore != nil) { n = n + 1; }
    vkCreateQueryPool = gdpa(device, "vkCreateQueryPool"):~fun(types.Device, *structs.QueryPoolCreateInfo, *structs.AllocationCallbacks, *types.QueryPool) i32;
    if (vkCreateQueryPool != nil) { n = n + 1; }
    vkDestroyQueryPool = gdpa(device, "vkDestroyQueryPool"):~fun(types.Device, types.QueryPool, *structs.AllocationCallbacks);
    if (vkDestroyQueryPool != nil) { n = n + 1; }
    vkGetQueryPoolResults = gdpa(device, "vkGetQueryPoolResults"):~fun(types.Device, types.QueryPool, u32, u32, u64, ptr, u64, u32) i32;
    if (vkGetQueryPoolResults != nil) { n = n + 1; }
    vkCreateBuffer = gdpa(device, "vkCreateBuffer"):~fun(types.Device, *structs.BufferCreateInfo, *structs.AllocationCallbacks, *types.Buffer) i32;
    if (vkCreateBuffer != nil) { n = n + 1; }
    vkDestroyBuffer = gdpa(device, "vkDestroyBuffer"):~fun(types.Device, types.Buffer, *structs.AllocationCallbacks);
    if (vkDestroyBuffer != nil) { n = n + 1; }
    vkCreateImage = gdpa(device, "vkCreateImage"):~fun(types.Device, *structs.ImageCreateInfo, *structs.AllocationCallbacks, *types.Image) i32;
    if (vkCreateImage != nil) { n = n + 1; }
    vkDestroyImage = gdpa(device, "vkDestroyImage"):~fun(types.Device, types.Image, *structs.AllocationCallbacks);
    if (vkDestroyImage != nil) { n = n + 1; }
    vkGetImageSubresourceLayout = gdpa(device, "vkGetImageSubresourceLayout"):~fun(types.Device, types.Image, *structs.ImageSubresource, *structs.SubresourceLayout);
    if (vkGetImageSubresourceLayout != nil) { n = n + 1; }
    vkCreateImageView = gdpa(device, "vkCreateImageView"):~fun(types.Device, *structs.ImageViewCreateInfo, *structs.AllocationCallbacks, *types.ImageView) i32;
    if (vkCreateImageView != nil) { n = n + 1; }
    vkDestroyImageView = gdpa(device, "vkDestroyImageView"):~fun(types.Device, types.ImageView, *structs.AllocationCallbacks);
    if (vkDestroyImageView != nil) { n = n + 1; }
    vkCreateCommandPool = gdpa(device, "vkCreateCommandPool"):~fun(types.Device, *structs.CommandPoolCreateInfo, *structs.AllocationCallbacks, *types.CommandPool) i32;
    if (vkCreateCommandPool != nil) { n = n + 1; }
    vkDestroyCommandPool = gdpa(device, "vkDestroyCommandPool"):~fun(types.Device, types.CommandPool, *structs.AllocationCallbacks);
    if (vkDestroyCommandPool != nil) { n = n + 1; }
    vkResetCommandPool = gdpa(device, "vkResetCommandPool"):~fun(types.Device, types.CommandPool, u32) i32;
    if (vkResetCommandPool != nil) { n = n + 1; }
    vkAllocateCommandBuffers = gdpa(device, "vkAllocateCommandBuffers"):~fun(types.Device, *structs.CommandBufferAllocateInfo, *types.CommandBuffer) i32;
    if (vkAllocateCommandBuffers != nil) { n = n + 1; }
    vkFreeCommandBuffers = gdpa(device, "vkFreeCommandBuffers"):~fun(types.Device, types.CommandPool, u32, *types.CommandBuffer);
    if (vkFreeCommandBuffers != nil) { n = n + 1; }
    vkBeginCommandBuffer = gdpa(device, "vkBeginCommandBuffer"):~fun(types.CommandBuffer, *structs.CommandBufferBeginInfo) i32;
    if (vkBeginCommandBuffer != nil) { n = n + 1; }
    vkEndCommandBuffer = gdpa(device, "vkEndCommandBuffer"):~fun(types.CommandBuffer) i32;
    if (vkEndCommandBuffer != nil) { n = n + 1; }
    vkResetCommandBuffer = gdpa(device, "vkResetCommandBuffer"):~fun(types.CommandBuffer, u32) i32;
    if (vkResetCommandBuffer != nil) { n = n + 1; }
    vkCmdCopyBuffer = gdpa(device, "vkCmdCopyBuffer"):~fun(types.CommandBuffer, types.Buffer, types.Buffer, u32, *structs.BufferCopy);
    if (vkCmdCopyBuffer != nil) { n = n + 1; }
    vkCmdCopyImage = gdpa(device, "vkCmdCopyImage"):~fun(types.CommandBuffer, types.Image, i32, types.Image, i32, u32, *structs.ImageCopy);
    if (vkCmdCopyImage != nil) { n = n + 1; }
    vkCmdCopyBufferToImage = gdpa(device, "vkCmdCopyBufferToImage"):~fun(types.CommandBuffer, types.Buffer, types.Image, i32, u32, *structs.BufferImageCopy);
    if (vkCmdCopyBufferToImage != nil) { n = n + 1; }
    vkCmdCopyImageToBuffer = gdpa(device, "vkCmdCopyImageToBuffer"):~fun(types.CommandBuffer, types.Image, i32, types.Buffer, u32, *structs.BufferImageCopy);
    if (vkCmdCopyImageToBuffer != nil) { n = n + 1; }
    vkCmdUpdateBuffer = gdpa(device, "vkCmdUpdateBuffer"):~fun(types.CommandBuffer, types.Buffer, u64, u64, ptr);
    if (vkCmdUpdateBuffer != nil) { n = n + 1; }
    vkCmdFillBuffer = gdpa(device, "vkCmdFillBuffer"):~fun(types.CommandBuffer, types.Buffer, u64, u64, u32);
    if (vkCmdFillBuffer != nil) { n = n + 1; }
    vkCmdPipelineBarrier = gdpa(device, "vkCmdPipelineBarrier"):~fun(types.CommandBuffer, u32, u32, u32, u32, *structs.MemoryBarrier, u32, *structs.BufferMemoryBarrier, u32, *structs.ImageMemoryBarrier);
    if (vkCmdPipelineBarrier != nil) { n = n + 1; }
    vkCmdBeginQuery = gdpa(device, "vkCmdBeginQuery"):~fun(types.CommandBuffer, types.QueryPool, u32, u32);
    if (vkCmdBeginQuery != nil) { n = n + 1; }
    vkCmdEndQuery = gdpa(device, "vkCmdEndQuery"):~fun(types.CommandBuffer, types.QueryPool, u32);
    if (vkCmdEndQuery != nil) { n = n + 1; }
    vkCmdResetQueryPool = gdpa(device, "vkCmdResetQueryPool"):~fun(types.CommandBuffer, types.QueryPool, u32, u32);
    if (vkCmdResetQueryPool != nil) { n = n + 1; }
    vkCmdWriteTimestamp = gdpa(device, "vkCmdWriteTimestamp"):~fun(types.CommandBuffer, i32, types.QueryPool, u32);
    if (vkCmdWriteTimestamp != nil) { n = n + 1; }
    vkCmdCopyQueryPoolResults = gdpa(device, "vkCmdCopyQueryPoolResults"):~fun(types.CommandBuffer, types.QueryPool, u32, u32, types.Buffer, u64, u64, u32);
    if (vkCmdCopyQueryPoolResults != nil) { n = n + 1; }
    vkCmdExecuteCommands = gdpa(device, "vkCmdExecuteCommands"):~fun(types.CommandBuffer, u32, *types.CommandBuffer);
    if (vkCmdExecuteCommands != nil) { n = n + 1; }
    vkCreateEvent = gdpa(device, "vkCreateEvent"):~fun(types.Device, *structs.EventCreateInfo, *structs.AllocationCallbacks, *types.Event) i32;
    if (vkCreateEvent != nil) { n = n + 1; }
    vkDestroyEvent = gdpa(device, "vkDestroyEvent"):~fun(types.Device, types.Event, *structs.AllocationCallbacks);
    if (vkDestroyEvent != nil) { n = n + 1; }
    vkGetEventStatus = gdpa(device, "vkGetEventStatus"):~fun(types.Device, types.Event) i32;
    if (vkGetEventStatus != nil) { n = n + 1; }
    vkSetEvent = gdpa(device, "vkSetEvent"):~fun(types.Device, types.Event) i32;
    if (vkSetEvent != nil) { n = n + 1; }
    vkResetEvent = gdpa(device, "vkResetEvent"):~fun(types.Device, types.Event) i32;
    if (vkResetEvent != nil) { n = n + 1; }
    vkCreateBufferView = gdpa(device, "vkCreateBufferView"):~fun(types.Device, *structs.BufferViewCreateInfo, *structs.AllocationCallbacks, *types.BufferView) i32;
    if (vkCreateBufferView != nil) { n = n + 1; }
    vkDestroyBufferView = gdpa(device, "vkDestroyBufferView"):~fun(types.Device, types.BufferView, *structs.AllocationCallbacks);
    if (vkDestroyBufferView != nil) { n = n + 1; }
    vkCreateShaderModule = gdpa(device, "vkCreateShaderModule"):~fun(types.Device, *structs.ShaderModuleCreateInfo, *structs.AllocationCallbacks, *types.ShaderModule) i32;
    if (vkCreateShaderModule != nil) { n = n + 1; }
    vkDestroyShaderModule = gdpa(device, "vkDestroyShaderModule"):~fun(types.Device, types.ShaderModule, *structs.AllocationCallbacks);
    if (vkDestroyShaderModule != nil) { n = n + 1; }
    vkCreatePipelineCache = gdpa(device, "vkCreatePipelineCache"):~fun(types.Device, *structs.PipelineCacheCreateInfo, *structs.AllocationCallbacks, *types.PipelineCache) i32;
    if (vkCreatePipelineCache != nil) { n = n + 1; }
    vkDestroyPipelineCache = gdpa(device, "vkDestroyPipelineCache"):~fun(types.Device, types.PipelineCache, *structs.AllocationCallbacks);
    if (vkDestroyPipelineCache != nil) { n = n + 1; }
    vkGetPipelineCacheData = gdpa(device, "vkGetPipelineCacheData"):~fun(types.Device, types.PipelineCache, *u64, ptr) i32;
    if (vkGetPipelineCacheData != nil) { n = n + 1; }
    vkMergePipelineCaches = gdpa(device, "vkMergePipelineCaches"):~fun(types.Device, types.PipelineCache, u32, *types.PipelineCache) i32;
    if (vkMergePipelineCaches != nil) { n = n + 1; }
    vkCreateComputePipelines = gdpa(device, "vkCreateComputePipelines"):~fun(types.Device, types.PipelineCache, u32, *structs.ComputePipelineCreateInfo, *structs.AllocationCallbacks, *types.Pipeline) i32;
    if (vkCreateComputePipelines != nil) { n = n + 1; }
    vkDestroyPipeline = gdpa(device, "vkDestroyPipeline"):~fun(types.Device, types.Pipeline, *structs.AllocationCallbacks);
    if (vkDestroyPipeline != nil) { n = n + 1; }
    vkCreatePipelineLayout = gdpa(device, "vkCreatePipelineLayout"):~fun(types.Device, *structs.PipelineLayoutCreateInfo, *structs.AllocationCallbacks, *types.PipelineLayout) i32;
    if (vkCreatePipelineLayout != nil) { n = n + 1; }
    vkDestroyPipelineLayout = gdpa(device, "vkDestroyPipelineLayout"):~fun(types.Device, types.PipelineLayout, *structs.AllocationCallbacks);
    if (vkDestroyPipelineLayout != nil) { n = n + 1; }
    vkCreateSampler = gdpa(device, "vkCreateSampler"):~fun(types.Device, *structs.SamplerCreateInfo, *structs.AllocationCallbacks, *types.Sampler) i32;
    if (vkCreateSampler != nil) { n = n + 1; }
    vkDestroySampler = gdpa(device, "vkDestroySampler"):~fun(types.Device, types.Sampler, *structs.AllocationCallbacks);
    if (vkDestroySampler != nil) { n = n + 1; }
    vkCreateDescriptorSetLayout = gdpa(device, "vkCreateDescriptorSetLayout"):~fun(types.Device, *structs.DescriptorSetLayoutCreateInfo, *structs.AllocationCallbacks, *types.DescriptorSetLayout) i32;
    if (vkCreateDescriptorSetLayout != nil) { n = n + 1; }
    vkDestroyDescriptorSetLayout = gdpa(device, "vkDestroyDescriptorSetLayout"):~fun(types.Device, types.DescriptorSetLayout, *structs.AllocationCallbacks);
    if (vkDestroyDescriptorSetLayout != nil) { n = n + 1; }
    vkCreateDescriptorPool = gdpa(device, "vkCreateDescriptorPool"):~fun(types.Device, *structs.DescriptorPoolCreateInfo, *structs.AllocationCallbacks, *types.DescriptorPool) i32;
    if (vkCreateDescriptorPool != nil) { n = n + 1; }
    vkDestroyDescriptorPool = gdpa(device, "vkDestroyDescriptorPool"):~fun(types.Device, types.DescriptorPool, *structs.AllocationCallbacks);
    if (vkDestroyDescriptorPool != nil) { n = n + 1; }
    vkResetDescriptorPool = gdpa(device, "vkResetDescriptorPool"):~fun(types.Device, types.DescriptorPool, u32) i32;
    if (vkResetDescriptorPool != nil) { n = n + 1; }
    vkAllocateDescriptorSets = gdpa(device, "vkAllocateDescriptorSets"):~fun(types.Device, *structs.DescriptorSetAllocateInfo, *types.DescriptorSet) i32;
    if (vkAllocateDescriptorSets != nil) { n = n + 1; }
    vkFreeDescriptorSets = gdpa(device, "vkFreeDescriptorSets"):~fun(types.Device, types.DescriptorPool, u32, *types.DescriptorSet) i32;
    if (vkFreeDescriptorSets != nil) { n = n + 1; }
    vkUpdateDescriptorSets = gdpa(device, "vkUpdateDescriptorSets"):~fun(types.Device, u32, *structs.WriteDescriptorSet, u32, *structs.CopyDescriptorSet);
    if (vkUpdateDescriptorSets != nil) { n = n + 1; }
    vkCmdBindPipeline = gdpa(device, "vkCmdBindPipeline"):~fun(types.CommandBuffer, i32, types.Pipeline);
    if (vkCmdBindPipeline != nil) { n = n + 1; }
    vkCmdBindDescriptorSets = gdpa(device, "vkCmdBindDescriptorSets"):~fun(types.CommandBuffer, i32, types.PipelineLayout, u32, u32, *types.DescriptorSet, u32, *u32);
    if (vkCmdBindDescriptorSets != nil) { n = n + 1; }
    vkCmdClearColorImage = gdpa(device, "vkCmdClearColorImage"):~fun(types.CommandBuffer, types.Image, i32, *structs.ClearColorValue, u32, *structs.ImageSubresourceRange);
    if (vkCmdClearColorImage != nil) { n = n + 1; }
    vkCmdDispatch = gdpa(device, "vkCmdDispatch"):~fun(types.CommandBuffer, u32, u32, u32);
    if (vkCmdDispatch != nil) { n = n + 1; }
    vkCmdDispatchIndirect = gdpa(device, "vkCmdDispatchIndirect"):~fun(types.CommandBuffer, types.Buffer, u64);
    if (vkCmdDispatchIndirect != nil) { n = n + 1; }
    vkCmdSetEvent = gdpa(device, "vkCmdSetEvent"):~fun(types.CommandBuffer, types.Event, u32);
    if (vkCmdSetEvent != nil) { n = n + 1; }
    vkCmdResetEvent = gdpa(device, "vkCmdResetEvent"):~fun(types.CommandBuffer, types.Event, u32);
    if (vkCmdResetEvent != nil) { n = n + 1; }
    vkCmdWaitEvents = gdpa(device, "vkCmdWaitEvents"):~fun(types.CommandBuffer, u32, *types.Event, u32, u32, u32, *structs.MemoryBarrier, u32, *structs.BufferMemoryBarrier, u32, *structs.ImageMemoryBarrier);
    if (vkCmdWaitEvents != nil) { n = n + 1; }
    vkCmdPushConstants = gdpa(device, "vkCmdPushConstants"):~fun(types.CommandBuffer, types.PipelineLayout, u32, u32, u32, ptr);
    if (vkCmdPushConstants != nil) { n = n + 1; }
    vkCreateGraphicsPipelines = gdpa(device, "vkCreateGraphicsPipelines"):~fun(types.Device, types.PipelineCache, u32, *structs.GraphicsPipelineCreateInfo, *structs.AllocationCallbacks, *types.Pipeline) i32;
    if (vkCreateGraphicsPipelines != nil) { n = n + 1; }
    vkCreateFramebuffer = gdpa(device, "vkCreateFramebuffer"):~fun(types.Device, *structs.FramebufferCreateInfo, *structs.AllocationCallbacks, *types.Framebuffer) i32;
    if (vkCreateFramebuffer != nil) { n = n + 1; }
    vkDestroyFramebuffer = gdpa(device, "vkDestroyFramebuffer"):~fun(types.Device, types.Framebuffer, *structs.AllocationCallbacks);
    if (vkDestroyFramebuffer != nil) { n = n + 1; }
    vkCreateRenderPass = gdpa(device, "vkCreateRenderPass"):~fun(types.Device, *structs.RenderPassCreateInfo, *structs.AllocationCallbacks, *types.RenderPass) i32;
    if (vkCreateRenderPass != nil) { n = n + 1; }
    vkDestroyRenderPass = gdpa(device, "vkDestroyRenderPass"):~fun(types.Device, types.RenderPass, *structs.AllocationCallbacks);
    if (vkDestroyRenderPass != nil) { n = n + 1; }
    vkGetRenderAreaGranularity = gdpa(device, "vkGetRenderAreaGranularity"):~fun(types.Device, types.RenderPass, *structs.Extent2D);
    if (vkGetRenderAreaGranularity != nil) { n = n + 1; }
    vkCmdSetViewport = gdpa(device, "vkCmdSetViewport"):~fun(types.CommandBuffer, u32, u32, *structs.Viewport);
    if (vkCmdSetViewport != nil) { n = n + 1; }
    vkCmdSetScissor = gdpa(device, "vkCmdSetScissor"):~fun(types.CommandBuffer, u32, u32, *structs.Rect2D);
    if (vkCmdSetScissor != nil) { n = n + 1; }
    vkCmdSetLineWidth = gdpa(device, "vkCmdSetLineWidth"):~fun(types.CommandBuffer, f32);
    if (vkCmdSetLineWidth != nil) { n = n + 1; }
    vkCmdSetDepthBias = gdpa(device, "vkCmdSetDepthBias"):~fun(types.CommandBuffer, f32, f32, f32);
    if (vkCmdSetDepthBias != nil) { n = n + 1; }
    vkCmdSetBlendConstants = gdpa(device, "vkCmdSetBlendConstants"):~fun(types.CommandBuffer, [4]f32);
    if (vkCmdSetBlendConstants != nil) { n = n + 1; }
    vkCmdSetDepthBounds = gdpa(device, "vkCmdSetDepthBounds"):~fun(types.CommandBuffer, f32, f32);
    if (vkCmdSetDepthBounds != nil) { n = n + 1; }
    vkCmdSetStencilCompareMask = gdpa(device, "vkCmdSetStencilCompareMask"):~fun(types.CommandBuffer, u32, u32);
    if (vkCmdSetStencilCompareMask != nil) { n = n + 1; }
    vkCmdSetStencilWriteMask = gdpa(device, "vkCmdSetStencilWriteMask"):~fun(types.CommandBuffer, u32, u32);
    if (vkCmdSetStencilWriteMask != nil) { n = n + 1; }
    vkCmdSetStencilReference = gdpa(device, "vkCmdSetStencilReference"):~fun(types.CommandBuffer, u32, u32);
    if (vkCmdSetStencilReference != nil) { n = n + 1; }
    vkCmdBindIndexBuffer = gdpa(device, "vkCmdBindIndexBuffer"):~fun(types.CommandBuffer, types.Buffer, u64, i32);
    if (vkCmdBindIndexBuffer != nil) { n = n + 1; }
    vkCmdBindVertexBuffers = gdpa(device, "vkCmdBindVertexBuffers"):~fun(types.CommandBuffer, u32, u32, *types.Buffer, *u64);
    if (vkCmdBindVertexBuffers != nil) { n = n + 1; }
    vkCmdDraw = gdpa(device, "vkCmdDraw"):~fun(types.CommandBuffer, u32, u32, u32, u32);
    if (vkCmdDraw != nil) { n = n + 1; }
    vkCmdDrawIndexed = gdpa(device, "vkCmdDrawIndexed"):~fun(types.CommandBuffer, u32, u32, u32, i32, u32);
    if (vkCmdDrawIndexed != nil) { n = n + 1; }
    vkCmdDrawIndirect = gdpa(device, "vkCmdDrawIndirect"):~fun(types.CommandBuffer, types.Buffer, u64, u32, u32);
    if (vkCmdDrawIndirect != nil) { n = n + 1; }
    vkCmdDrawIndexedIndirect = gdpa(device, "vkCmdDrawIndexedIndirect"):~fun(types.CommandBuffer, types.Buffer, u64, u32, u32);
    if (vkCmdDrawIndexedIndirect != nil) { n = n + 1; }
    vkCmdBlitImage = gdpa(device, "vkCmdBlitImage"):~fun(types.CommandBuffer, types.Image, i32, types.Image, i32, u32, *structs.ImageBlit, i32);
    if (vkCmdBlitImage != nil) { n = n + 1; }
    vkCmdClearDepthStencilImage = gdpa(device, "vkCmdClearDepthStencilImage"):~fun(types.CommandBuffer, types.Image, i32, *structs.ClearDepthStencilValue, u32, *structs.ImageSubresourceRange);
    if (vkCmdClearDepthStencilImage != nil) { n = n + 1; }
    vkCmdClearAttachments = gdpa(device, "vkCmdClearAttachments"):~fun(types.CommandBuffer, u32, *structs.ClearAttachment, u32, *structs.ClearRect);
    if (vkCmdClearAttachments != nil) { n = n + 1; }
    vkCmdResolveImage = gdpa(device, "vkCmdResolveImage"):~fun(types.CommandBuffer, types.Image, i32, types.Image, i32, u32, *structs.ImageResolve);
    if (vkCmdResolveImage != nil) { n = n + 1; }
    vkCmdBeginRenderPass = gdpa(device, "vkCmdBeginRenderPass"):~fun(types.CommandBuffer, *structs.RenderPassBeginInfo, i32);
    if (vkCmdBeginRenderPass != nil) { n = n + 1; }
    vkCmdNextSubpass = gdpa(device, "vkCmdNextSubpass"):~fun(types.CommandBuffer, i32);
    if (vkCmdNextSubpass != nil) { n = n + 1; }
    vkCmdEndRenderPass = gdpa(device, "vkCmdEndRenderPass"):~fun(types.CommandBuffer);
    if (vkCmdEndRenderPass != nil) { n = n + 1; }
    vkBindBufferMemory2 = gdpa(device, "vkBindBufferMemory2"):~fun(types.Device, u32, *structs.BindBufferMemoryInfo) i32;
    if (vkBindBufferMemory2 != nil) { n = n + 1; }
    vkBindImageMemory2 = gdpa(device, "vkBindImageMemory2"):~fun(types.Device, u32, *structs.BindImageMemoryInfo) i32;
    if (vkBindImageMemory2 != nil) { n = n + 1; }
    vkGetDeviceGroupPeerMemoryFeatures = gdpa(device, "vkGetDeviceGroupPeerMemoryFeatures"):~fun(types.Device, u32, u32, u32, *u32);
    if (vkGetDeviceGroupPeerMemoryFeatures != nil) { n = n + 1; }
    vkCmdSetDeviceMask = gdpa(device, "vkCmdSetDeviceMask"):~fun(types.CommandBuffer, u32);
    if (vkCmdSetDeviceMask != nil) { n = n + 1; }
    vkGetImageMemoryRequirements2 = gdpa(device, "vkGetImageMemoryRequirements2"):~fun(types.Device, *structs.ImageMemoryRequirementsInfo2, *structs.MemoryRequirements2);
    if (vkGetImageMemoryRequirements2 != nil) { n = n + 1; }
    vkGetBufferMemoryRequirements2 = gdpa(device, "vkGetBufferMemoryRequirements2"):~fun(types.Device, *structs.BufferMemoryRequirementsInfo2, *structs.MemoryRequirements2);
    if (vkGetBufferMemoryRequirements2 != nil) { n = n + 1; }
    vkGetImageSparseMemoryRequirements2 = gdpa(device, "vkGetImageSparseMemoryRequirements2"):~fun(types.Device, *structs.ImageSparseMemoryRequirementsInfo2, *u32, *structs.SparseImageMemoryRequirements2);
    if (vkGetImageSparseMemoryRequirements2 != nil) { n = n + 1; }
    vkTrimCommandPool = gdpa(device, "vkTrimCommandPool"):~fun(types.Device, types.CommandPool, u32);
    if (vkTrimCommandPool != nil) { n = n + 1; }
    vkGetDeviceQueue2 = gdpa(device, "vkGetDeviceQueue2"):~fun(types.Device, *structs.DeviceQueueInfo2, *types.Queue);
    if (vkGetDeviceQueue2 != nil) { n = n + 1; }
    vkCmdDispatchBase = gdpa(device, "vkCmdDispatchBase"):~fun(types.CommandBuffer, u32, u32, u32, u32, u32, u32);
    if (vkCmdDispatchBase != nil) { n = n + 1; }
    vkCreateDescriptorUpdateTemplate = gdpa(device, "vkCreateDescriptorUpdateTemplate"):~fun(types.Device, *structs.DescriptorUpdateTemplateCreateInfo, *structs.AllocationCallbacks, *types.DescriptorUpdateTemplate) i32;
    if (vkCreateDescriptorUpdateTemplate != nil) { n = n + 1; }
    vkDestroyDescriptorUpdateTemplate = gdpa(device, "vkDestroyDescriptorUpdateTemplate"):~fun(types.Device, types.DescriptorUpdateTemplate, *structs.AllocationCallbacks);
    if (vkDestroyDescriptorUpdateTemplate != nil) { n = n + 1; }
    vkUpdateDescriptorSetWithTemplate = gdpa(device, "vkUpdateDescriptorSetWithTemplate"):~fun(types.Device, types.DescriptorSet, types.DescriptorUpdateTemplate, ptr);
    if (vkUpdateDescriptorSetWithTemplate != nil) { n = n + 1; }
    vkGetDescriptorSetLayoutSupport = gdpa(device, "vkGetDescriptorSetLayoutSupport"):~fun(types.Device, *structs.DescriptorSetLayoutCreateInfo, *structs.DescriptorSetLayoutSupport);
    if (vkGetDescriptorSetLayoutSupport != nil) { n = n + 1; }
    vkCreateSamplerYcbcrConversion = gdpa(device, "vkCreateSamplerYcbcrConversion"):~fun(types.Device, *structs.SamplerYcbcrConversionCreateInfo, *structs.AllocationCallbacks, *types.SamplerYcbcrConversion) i32;
    if (vkCreateSamplerYcbcrConversion != nil) { n = n + 1; }
    vkDestroySamplerYcbcrConversion = gdpa(device, "vkDestroySamplerYcbcrConversion"):~fun(types.Device, types.SamplerYcbcrConversion, *structs.AllocationCallbacks);
    if (vkDestroySamplerYcbcrConversion != nil) { n = n + 1; }
    vkResetQueryPool = gdpa(device, "vkResetQueryPool"):~fun(types.Device, types.QueryPool, u32, u32);
    if (vkResetQueryPool != nil) { n = n + 1; }
    vkGetSemaphoreCounterValue = gdpa(device, "vkGetSemaphoreCounterValue"):~fun(types.Device, types.Semaphore, *u64) i32;
    if (vkGetSemaphoreCounterValue != nil) { n = n + 1; }
    vkWaitSemaphores = gdpa(device, "vkWaitSemaphores"):~fun(types.Device, *structs.SemaphoreWaitInfo, u64) i32;
    if (vkWaitSemaphores != nil) { n = n + 1; }
    vkSignalSemaphore = gdpa(device, "vkSignalSemaphore"):~fun(types.Device, *structs.SemaphoreSignalInfo) i32;
    if (vkSignalSemaphore != nil) { n = n + 1; }
    vkGetBufferDeviceAddress = gdpa(device, "vkGetBufferDeviceAddress"):~fun(types.Device, *structs.BufferDeviceAddressInfo) u64;
    if (vkGetBufferDeviceAddress != nil) { n = n + 1; }
    vkGetBufferOpaqueCaptureAddress = gdpa(device, "vkGetBufferOpaqueCaptureAddress"):~fun(types.Device, *structs.BufferDeviceAddressInfo) u64;
    if (vkGetBufferOpaqueCaptureAddress != nil) { n = n + 1; }
    vkGetDeviceMemoryOpaqueCaptureAddress = gdpa(device, "vkGetDeviceMemoryOpaqueCaptureAddress"):~fun(types.Device, *structs.DeviceMemoryOpaqueCaptureAddressInfo) u64;
    if (vkGetDeviceMemoryOpaqueCaptureAddress != nil) { n = n + 1; }
    vkCmdDrawIndirectCount = gdpa(device, "vkCmdDrawIndirectCount"):~fun(types.CommandBuffer, types.Buffer, u64, types.Buffer, u64, u32, u32);
    if (vkCmdDrawIndirectCount != nil) { n = n + 1; }
    vkCmdDrawIndexedIndirectCount = gdpa(device, "vkCmdDrawIndexedIndirectCount"):~fun(types.CommandBuffer, types.Buffer, u64, types.Buffer, u64, u32, u32);
    if (vkCmdDrawIndexedIndirectCount != nil) { n = n + 1; }
    vkCreateRenderPass2 = gdpa(device, "vkCreateRenderPass2"):~fun(types.Device, *structs.RenderPassCreateInfo2, *structs.AllocationCallbacks, *types.RenderPass) i32;
    if (vkCreateRenderPass2 != nil) { n = n + 1; }
    vkCmdBeginRenderPass2 = gdpa(device, "vkCmdBeginRenderPass2"):~fun(types.CommandBuffer, *structs.RenderPassBeginInfo, *structs.SubpassBeginInfo);
    if (vkCmdBeginRenderPass2 != nil) { n = n + 1; }
    vkCmdNextSubpass2 = gdpa(device, "vkCmdNextSubpass2"):~fun(types.CommandBuffer, *structs.SubpassBeginInfo, *structs.SubpassEndInfo);
    if (vkCmdNextSubpass2 != nil) { n = n + 1; }
    vkCmdEndRenderPass2 = gdpa(device, "vkCmdEndRenderPass2"):~fun(types.CommandBuffer, *structs.SubpassEndInfo);
    if (vkCmdEndRenderPass2 != nil) { n = n + 1; }
    vkCreatePrivateDataSlot = gdpa(device, "vkCreatePrivateDataSlot"):~fun(types.Device, *structs.PrivateDataSlotCreateInfo, *structs.AllocationCallbacks, *types.PrivateDataSlot) i32;
    if (vkCreatePrivateDataSlot != nil) { n = n + 1; }
    vkDestroyPrivateDataSlot = gdpa(device, "vkDestroyPrivateDataSlot"):~fun(types.Device, types.PrivateDataSlot, *structs.AllocationCallbacks);
    if (vkDestroyPrivateDataSlot != nil) { n = n + 1; }
    vkSetPrivateData = gdpa(device, "vkSetPrivateData"):~fun(types.Device, i32, u64, types.PrivateDataSlot, u64) i32;
    if (vkSetPrivateData != nil) { n = n + 1; }
    vkGetPrivateData = gdpa(device, "vkGetPrivateData"):~fun(types.Device, i32, u64, types.PrivateDataSlot, *u64);
    if (vkGetPrivateData != nil) { n = n + 1; }
    vkCmdPipelineBarrier2 = gdpa(device, "vkCmdPipelineBarrier2"):~fun(types.CommandBuffer, *structs.DependencyInfo);
    if (vkCmdPipelineBarrier2 != nil) { n = n + 1; }
    vkCmdWriteTimestamp2 = gdpa(device, "vkCmdWriteTimestamp2"):~fun(types.CommandBuffer, u64, types.QueryPool, u32);
    if (vkCmdWriteTimestamp2 != nil) { n = n + 1; }
    vkQueueSubmit2 = gdpa(device, "vkQueueSubmit2"):~fun(types.Queue, u32, *structs.SubmitInfo2, types.Fence) i32;
    if (vkQueueSubmit2 != nil) { n = n + 1; }
    vkCmdCopyBuffer2 = gdpa(device, "vkCmdCopyBuffer2"):~fun(types.CommandBuffer, *structs.CopyBufferInfo2);
    if (vkCmdCopyBuffer2 != nil) { n = n + 1; }
    vkCmdCopyImage2 = gdpa(device, "vkCmdCopyImage2"):~fun(types.CommandBuffer, *structs.CopyImageInfo2);
    if (vkCmdCopyImage2 != nil) { n = n + 1; }
    vkCmdCopyBufferToImage2 = gdpa(device, "vkCmdCopyBufferToImage2"):~fun(types.CommandBuffer, *structs.CopyBufferToImageInfo2);
    if (vkCmdCopyBufferToImage2 != nil) { n = n + 1; }
    vkCmdCopyImageToBuffer2 = gdpa(device, "vkCmdCopyImageToBuffer2"):~fun(types.CommandBuffer, *structs.CopyImageToBufferInfo2);
    if (vkCmdCopyImageToBuffer2 != nil) { n = n + 1; }
    vkGetDeviceBufferMemoryRequirements = gdpa(device, "vkGetDeviceBufferMemoryRequirements"):~fun(types.Device, *structs.DeviceBufferMemoryRequirements, *structs.MemoryRequirements2);
    if (vkGetDeviceBufferMemoryRequirements != nil) { n = n + 1; }
    vkGetDeviceImageMemoryRequirements = gdpa(device, "vkGetDeviceImageMemoryRequirements"):~fun(types.Device, *structs.DeviceImageMemoryRequirements, *structs.MemoryRequirements2);
    if (vkGetDeviceImageMemoryRequirements != nil) { n = n + 1; }
    vkGetDeviceImageSparseMemoryRequirements = gdpa(device, "vkGetDeviceImageSparseMemoryRequirements"):~fun(types.Device, *structs.DeviceImageMemoryRequirements, *u32, *structs.SparseImageMemoryRequirements2);
    if (vkGetDeviceImageSparseMemoryRequirements != nil) { n = n + 1; }
    vkCmdSetEvent2 = gdpa(device, "vkCmdSetEvent2"):~fun(types.CommandBuffer, types.Event, *structs.DependencyInfo);
    if (vkCmdSetEvent2 != nil) { n = n + 1; }
    vkCmdResetEvent2 = gdpa(device, "vkCmdResetEvent2"):~fun(types.CommandBuffer, types.Event, u64);
    if (vkCmdResetEvent2 != nil) { n = n + 1; }
    vkCmdWaitEvents2 = gdpa(device, "vkCmdWaitEvents2"):~fun(types.CommandBuffer, u32, *types.Event, *structs.DependencyInfo);
    if (vkCmdWaitEvents2 != nil) { n = n + 1; }
    vkCmdBlitImage2 = gdpa(device, "vkCmdBlitImage2"):~fun(types.CommandBuffer, *structs.BlitImageInfo2);
    if (vkCmdBlitImage2 != nil) { n = n + 1; }
    vkCmdResolveImage2 = gdpa(device, "vkCmdResolveImage2"):~fun(types.CommandBuffer, *structs.ResolveImageInfo2);
    if (vkCmdResolveImage2 != nil) { n = n + 1; }
    vkCmdBeginRendering = gdpa(device, "vkCmdBeginRendering"):~fun(types.CommandBuffer, *structs.RenderingInfo);
    if (vkCmdBeginRendering != nil) { n = n + 1; }
    vkCmdEndRendering = gdpa(device, "vkCmdEndRendering"):~fun(types.CommandBuffer);
    if (vkCmdEndRendering != nil) { n = n + 1; }
    vkCmdSetCullMode = gdpa(device, "vkCmdSetCullMode"):~fun(types.CommandBuffer, u32);
    if (vkCmdSetCullMode != nil) { n = n + 1; }
    vkCmdSetFrontFace = gdpa(device, "vkCmdSetFrontFace"):~fun(types.CommandBuffer, i32);
    if (vkCmdSetFrontFace != nil) { n = n + 1; }
    vkCmdSetPrimitiveTopology = gdpa(device, "vkCmdSetPrimitiveTopology"):~fun(types.CommandBuffer, i32);
    if (vkCmdSetPrimitiveTopology != nil) { n = n + 1; }
    vkCmdSetViewportWithCount = gdpa(device, "vkCmdSetViewportWithCount"):~fun(types.CommandBuffer, u32, *structs.Viewport);
    if (vkCmdSetViewportWithCount != nil) { n = n + 1; }
    vkCmdSetScissorWithCount = gdpa(device, "vkCmdSetScissorWithCount"):~fun(types.CommandBuffer, u32, *structs.Rect2D);
    if (vkCmdSetScissorWithCount != nil) { n = n + 1; }
    vkCmdBindVertexBuffers2 = gdpa(device, "vkCmdBindVertexBuffers2"):~fun(types.CommandBuffer, u32, u32, *types.Buffer, *u64, *u64, *u64);
    if (vkCmdBindVertexBuffers2 != nil) { n = n + 1; }
    vkCmdSetDepthTestEnable = gdpa(device, "vkCmdSetDepthTestEnable"):~fun(types.CommandBuffer, u32);
    if (vkCmdSetDepthTestEnable != nil) { n = n + 1; }
    vkCmdSetDepthWriteEnable = gdpa(device, "vkCmdSetDepthWriteEnable"):~fun(types.CommandBuffer, u32);
    if (vkCmdSetDepthWriteEnable != nil) { n = n + 1; }
    vkCmdSetDepthCompareOp = gdpa(device, "vkCmdSetDepthCompareOp"):~fun(types.CommandBuffer, i32);
    if (vkCmdSetDepthCompareOp != nil) { n = n + 1; }
    vkCmdSetDepthBoundsTestEnable = gdpa(device, "vkCmdSetDepthBoundsTestEnable"):~fun(types.CommandBuffer, u32);
    if (vkCmdSetDepthBoundsTestEnable != nil) { n = n + 1; }
    vkCmdSetStencilTestEnable = gdpa(device, "vkCmdSetStencilTestEnable"):~fun(types.CommandBuffer, u32);
    if (vkCmdSetStencilTestEnable != nil) { n = n + 1; }
    vkCmdSetStencilOp = gdpa(device, "vkCmdSetStencilOp"):~fun(types.CommandBuffer, u32, i32, i32, i32, i32);
    if (vkCmdSetStencilOp != nil) { n = n + 1; }
    vkCmdSetRasterizerDiscardEnable = gdpa(device, "vkCmdSetRasterizerDiscardEnable"):~fun(types.CommandBuffer, u32);
    if (vkCmdSetRasterizerDiscardEnable != nil) { n = n + 1; }
    vkCmdSetDepthBiasEnable = gdpa(device, "vkCmdSetDepthBiasEnable"):~fun(types.CommandBuffer, u32);
    if (vkCmdSetDepthBiasEnable != nil) { n = n + 1; }
    vkCmdSetPrimitiveRestartEnable = gdpa(device, "vkCmdSetPrimitiveRestartEnable"):~fun(types.CommandBuffer, u32);
    if (vkCmdSetPrimitiveRestartEnable != nil) { n = n + 1; }
    ret n;
}

fun gipa_nil(instance: ptr, name: *u8) ptr {
    ret nil;
}

var load_calls: i64 = 0;

fun gipa_count(instance: ptr, name: *u8) ptr {
    load_calls = load_calls + 1;
    ret (?load_calls)::ptr;
}

fun gdpa_count(device: ptr, name: *u8) ptr {
    load_calls = load_calls + 1;
    ret (?load_calls)::ptr;
}

# a nil loader resolves nothing and leaves the global table nil
test "c: load_global with a nil loader resolves nothing" {
    val n: i64 = load_global(gipa_nil);
    if (n != 0) { ret 1; }
    if (vkCreateInstance != nil) { ret 1; }
    ret 0;
}

# a stub loader that answers every name resolves each tier fully; the count
# equals the number of loader calls for that tier
test "c: a stub loader resolves every command in each tier" {
    load_calls = 0;
    val g: i64 = load_global(gipa_count);
    if (g != load_calls) { ret 1; }
    if (g <= 0) { ret 1; }
    load_calls = 0;
    val i: i64 = load_instance(gipa_count, (?load_calls)::ptr);
    if (i != load_calls) { ret 1; }
    if (i <= 0) { ret 1; }
    load_calls = 0;
    val d: i64 = load_device(gdpa_count, (?load_calls)::ptr);
    if (d != load_calls) { ret 1; }
    if (d <= 0) { ret 1; }
    ret 0;
}

var abi_seen: u32 = 0;

fun abi_fake(api: *u32) i32 {
    @api = abi_seen;
    ret 0;
}

# pin the loaded-pointer call ABI: a mach fake stands in for a command and the
# call must deliver the out-pointer and read its result back without a loader
test "c: loaded-pointer call ABI" {
    abi_seen = 0x00403000;
    vkEnumerateInstanceVersion = abi_fake;
    var ver: u32 = 0;
    val r: i32 = vkEnumerateInstanceVersion(?ver);
    if (r != 0) { ret 1; }
    if (ver != 0x00403000) { ret 1; }
    ret 0;
}
